---
phase: quick-260609-0tc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app.ts]
autonomous: true
requirements: [issue-219]
must_haves:
  truths:
    - "Tapping the disabled 0 in the hundreds box opens the same popover style the (i) clue icons use"
    - "The popover reads: The number is 100–999, so the first digit can't be 0."
    - "The existing (i) clue tooltips still open/close/position exactly as before"
    - "The hundreds-box 0 is keyboard-focusable; Enter or Space opens the tip"
    - "All other eliminated digits keep their current (no-tip) behavior"
  artifacts:
    - path: "src/app.ts"
      provides: "Generic showTip() + thin showTagTip() wrapper + hundreds-box 0 click handler"
      contains: "function showTip"
  key_links:
    - from: "buildKeypad (activeBox===0 && d===0)"
      to: "showTip"
      via: "click handler with exact copy string"
      pattern: "showTip\\("
    - from: "showTagTip"
      to: "showTip"
      via: "thin wrapper after TAG_TIPS lookup"
      pattern: "showTip\\(tip, anchor\\)"
---

<objective>
Implement GitHub issue #219. Tapping the disabled `0` in the hundreds digit box shows the
same overlay/popover the (i) clue-info icons use, explaining that the first digit can't be 0.

Purpose: The hundreds-box 0 is greyed out with no explanation today. Users don't know why.
Reuse the existing tooltip so the answer is one tap away, consistently styled.

Output: Refactored `showTagTip` (extracted generic `showTip`) and a keyboard-accessible
hundreds-box 0 that opens the tip. Frontend-only, `src/app.ts` only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@docs/CONVENTIONS.md
@docs/DA-REVIEW.md

<interfaces>
<!-- Current code the executor refactors. Extracted from src/app.ts — no exploration needed. -->

Current showTagTip (src/app.ts ~167-217) — extract the popover body into showTip(message, anchor):
  - closeTagTip() called first
  - track("tooltip_opened")
  - creates div.role="tooltip", data-tag-tip, Tailwind classes:
    "absolute left-0 bottom-full mb-2 min-w-[18rem] p-3 bg-surface rounded-md z-50"
  - innerHTML: close <button aria-label="Close"> with <use href="/sprites.svg#icon-circle-x"/>
    + <p class="text-base text-text leading-snug pr-6 font-[Quicksand]">${message}</p>
  - appends to anchor.parentElement (adds "relative"), sticky-header flip logic,
    outside-click + Escape close handlers, (popover as any)._cleanup hook
  closeTagTip() stays unchanged (queries [data-tag-tip], calls _cleanup, removes).

Current buildKeypad (src/app.ts ~364-389) — native <button> per digit:
  const disabled = activeBox === 0 && d === 0;
  const elim = disabled || !possibles[activeBox].has(d);
  // elim branch already greys the button (text-text/25)
  btn.setAttribute("aria-label", `Toggle digit ${d}`);
  if (disabled) { btn.disabled = true; } else { btn.addEventListener("click", () => toggleDigit(d)); }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract generic showTip() and make showTagTip() a thin wrapper</name>
  <files>src/app.ts</files>
  <action>
Extract the popover-building body of showTagTip(tag, anchor) into a new generic
`function showTip(message: string, anchor: HTMLElement): void`. showTip must reproduce
the CURRENT behavior byte-for-byte: call closeTagTip() first, call track("tooltip_opened"),
create the same role="tooltip" div with data-tag-tip and the identical Tailwind class string,
the same close-button innerHTML (#icon-circle-x), insert ${message} into the same
`<p class="text-base text-text leading-snug pr-6 font-[Quicksand]">`, append to
anchor.parentElement (add "relative"), run the same sticky-header flip logic
(compares popover rect.top to header bottom, removes bottom-full/mb-2, sets style.top
to anchor.offsetTop + anchor.offsetHeight + 8), wire the close button, outside-click
handler (ignores clicks on anchor), Escape handler, and the (popover as any)._cleanup hook.

Then reduce showTagTip(tag, anchor) to a thin wrapper:
  const tip = TAG_TIPS[tag];
  if (!tip) return;
  showTip(tip, anchor);
Do NOT move the TAG_TIPS lookup or the early-return into showTip — keep them in the wrapper
so the (i) path is unchanged when a tag is missing. closeTagTip() stays exactly as-is.
The (i) tagBtn wiring (showTagTip(tag, tagBtn)) is untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>showTip exists; showTagTip is a 3-line wrapper calling it; tsc clean; existing vitest suite green (regression guard — the (i) tooltip behavior is unchanged).</done>
</task>

<task type="auto">
  <name>Task 2: Make the hundreds-box 0 tappable and open the tip</name>
  <files>src/app.ts</files>
  <action>
In buildKeypad (~371-386), special-case the hundreds-box zero (activeBox === 0 && d === 0).
Keep `const disabled = activeBox === 0 && d === 0;` and `const elim = disabled || !possibles...`
so the existing `elim` branch keeps the greyed styling and the h-12 (48px) touch target.

Change the wiring block: instead of `if (disabled) { btn.disabled = true; }`, do NOT set
the native `disabled` property for this case (so the button stays focusable and tappable).
Instead:
  - btn.setAttribute("aria-disabled", "true")
  - set a meaningful aria-label overriding the generic one:
    btn.setAttribute("aria-label", "0 unavailable — the first digit can't be 0")
  - do NOT attach toggleDigit
  - attach a click handler:
    btn.addEventListener("click", () => showTip("The number is 100–999, so the first digit can't be 0.", btn))
The copy string must be EXACT (en-dash 100–999, straight apostrophe in can't).

All OTHER digits keep current behavior: the non-disabled branch still attaches
toggleDigit; player-eliminated digits (elim but not disabled) keep aria-pressed and remain
plain greyed buttons with the toggleDigit handler. This special handling is ONLY for
activeBox === 0 && d === 0.

Note (no code change needed): the keypad buttons are native <button> elements, so Enter and
Space fire the click handler natively — keyboard activation works once `disabled` is removed.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>The hundreds-box 0 has aria-disabled="true" (not native disabled), a descriptive aria-label, no toggleDigit, and a click handler opening showTip with the exact copy; other digits unchanged; tsc clean; vitest green.</done>
</task>

</tasks>

<verification>
QA scope (decided up front — proportional to a small frontend-only a11y change):

- `npx tsc --noEmit` — type check passes.
- `npx vitest run` — existing suite stays green. This is the regression guard on the
  showTagTip refactor. No NEW unit test is required: app.ts DOM is not unit-tested, and
  bootstrapping keypad/tooltip DOM + game state for one assertion is heavy for low value.
- In-browser preview verification (done by the orchestrator after execution):
  1. Open a daily puzzle, tap the hundreds (first) box to open the keypad.
  2. Tap the greyed `0` — confirm the popover appears reading
     "The number is 100–999, so the first digit can't be 0." and closes via the X,
     outside click, and Escape.
  3. Tap with keyboard (focus the 0, press Enter and Space) — confirm the tip opens.
  4. Confirm the (i) clue tooltips still open, position, and close exactly as before.
  5. Confirm other eliminated digits still toggle normally.

Accessibility (flag for DA review — this is the crux of the change):
- WCAG 4.1.2 Name, Role, Value: 0 uses aria-disabled="true" (not native disabled) so it
  stays in the tab order and is operable; descriptive aria-label set.
- WCAG 2.1.1 Keyboard: Enter/Space open the tip (native button activation).
- WCAG 2.5.5 / 2.5.8 Target Size: touch target stays h-12 (48px), unchanged.
- Popover already has role="tooltip" and a labelled Close button.
</verification>

<success_criteria>
- showTip(message, anchor) extracted; showTagTip is a thin wrapper; (i) behavior byte-for-byte equivalent.
- Hundreds-box 0 is focusable/tappable (aria-disabled, not native disabled), keyboard-operable, and opens the exact-copy tip.
- Other digits unchanged. tsc clean, vitest green. No worker/API changes, no new colour tokens, no id selectors, no new analytics event.
- DA review + self-review gates run before any PR (required: touches app.ts + accessibility, per CLAUDE.md).
</success_criteria>

<output>
Create `.planning/quick/260609-0tc-issue-219-hundreds-box-zero-tooltip/260609-0tc-SUMMARY.md` when done.
</output>
