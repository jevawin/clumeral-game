---
phase: 03-game-screen-menu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - public/sprites.svg
  - src/screens.ts
autonomous: true
requirements: [GAM-01, GAM-02, GAM-03, GAM-06, MNU-01, MNU-02, MNU-03, MNU-04, MNU-05]

must_haves:
  truths:
    - "Game screen section contains complete game HTML markup (header, clues, digits, keypad, submit, save, feedback, history, stats, next, again)"
    - "Old puzzle card markup is removed from index.html"
    - "Old footer links (Guide, Feedback, Archive, Theme) are removed from index.html"
    - "Hamburger menu dropdown markup exists in the game header"
    - "Skeleton loaders appear in the clue list area"
    - "screens.ts overlay guard for empty game section is removed"
  artifacts:
    - path: "index.html"
      provides: "Complete game screen HTML inside [data-screen='game']"
      contains: "data-game-header"
    - path: "index.html"
      provides: "No old puzzle card"
      missing: "id=\"puzzle\""
    - path: "public/sprites.svg"
      provides: "Hamburger menu icon"
      contains: "icon-menu"
    - path: "src/screens.ts"
      provides: "Clean overlay logic without empty-game guard"
      missing: "gameEmpty"
  key_links:
    - from: "index.html [data-screen='game']"
      to: "public/sprites.svg"
      via: "SVG use href"
      pattern: "sprites.svg#icon-menu"
---

<objective>
Build the complete game screen HTML markup inside `[data-screen="game"]` in `index.html`, remove all old game markup and legacy footer links, add a hamburger icon to the sprite sheet, and clean up the screens.ts overlay guard.

Purpose: This plan creates the structural foundation — all the HTML — that Plan 02 will wire up with JavaScript. Without this markup, Plan 02's DOM cache and render function updates have nothing to target.

Output: A fully populated game screen section in index.html with header, menu dropdown, clue skeletons, digit boxes, keypad, submit button, save row, feedback, history, stats, next puzzle, and random-again elements. Old markup removed. Hamburger icon added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-game-screen-menu/03-CONTEXT.md
@.planning/phases/03-game-screen-menu/03-UI-SPEC.md
@.planning/phases/03-game-screen-menu/03-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/02-welcome-how-to-play/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hamburger icon to sprites.svg and write game screen HTML into index.html</name>
  <files>public/sprites.svg, index.html</files>
  <read_first>
    - public/sprites.svg (to see existing icon format and add hamburger icon matching the same style)
    - index.html (full file — to see current structure, old markup to remove, and exact location of [data-screen="game"] shell)
    - .planning/phases/03-game-screen-menu/03-UI-SPEC.md (the visual contract — all HTML structure, classes, and data attributes are specified here)
    - .planning/phases/03-game-screen-menu/03-RESEARCH.md (code examples section has exact HTML snippets for header, menu, clue list)
    - src/style.css (reference for current clue, keypad, digit box markup structure — the new markup must have equivalent data attributes)
  </read_first>
  <action>
**sprites.svg — add hamburger icon:**

Add a new `<symbol id="icon-menu">` to `public/sprites.svg` matching the existing icon style (viewBox="0 0 24 24", fill="none", stroke="currentColor", stroke-width="2", stroke-linecap="round", stroke-linejoin="round"). The hamburger is three horizontal lines:

```xml
<symbol id="icon-menu" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
  <line x1="4" y1="6" x2="20" y2="6"/>
  <line x1="4" y1="12" x2="20" y2="12"/>
  <line x1="4" y1="18" x2="20" y2="18"/>
</symbol>
```

**index.html — remove old game markup:**

1. Delete the entire old puzzle card block: from `<div id="puzzle" class="card" data-card>` through its closing `</div>` (currently lines ~294-390). This includes: card label, clue list with skeletons, hint, digit boxes, keypad, submit, save row, feedback, history, stats, next puzzle, and random again.

2. Delete the old footer block: from `<footer class="footer">` through its closing `</footer>` (currently lines ~392-432). This includes: swatch list, footer links (Guide, Feedback, Archive, Theme toggle), footer body text, and dev links.

3. Delete the feedback header button block: from `<p class="fb-header">` through its closing `</p>` (currently lines ~286-291).

4. Delete the subtitle block: from `<p class="subtitle">` through its closing `</p>` (currently lines ~280-284). This copy is now on the welcome screen.

**index.html — populate [data-screen="game"]:**

Inside the existing `<section data-screen="game" ...>` (currently at line ~443), add `bg-bg` to the section's class list and replace the comment with the complete game screen HTML. The structure follows the UI-SPEC Layout Contract exactly:

```html
<!-- ── Game header ── -->
<header data-game-header class="h-14 sticky top-0 z-30 flex items-center justify-between px-4 bg-bg border-b border-border">
  <div class="flex items-center gap-2">
    <span class="text-xl font-bold text-text font-[DM_Sans]">Clumeral</span>
    <span data-plabel class="text-muted text-base font-[DM_Sans]"></span>
  </div>
  <button data-menu-btn class="h-10 w-10 flex items-center justify-center text-text" type="button" aria-expanded="false" aria-controls="game-menu" aria-label="Open menu">
    <svg width="24" height="24" aria-hidden="true"><use href="/sprites.svg#icon-menu"/></svg>
  </button>
</header>

<!-- ── Menu dropdown ── -->
<div id="game-menu" data-menu role="dialog" aria-label="Game menu"
     class="hidden absolute top-14 right-0 z-40 min-w-[200px] py-2 bg-surface border border-border rounded-lg shadow-lg">
  <button data-theme-toggle class="flex items-center gap-3 px-4 py-3 w-full text-left text-base text-text hover:bg-bg font-[DM_Sans]" type="button">
    <svg width="20" height="20" class="text-muted" aria-hidden="true"><use href="/sprites.svg#icon-moon"/></svg>
    <span data-theme-label>Dark mode</span>
  </button>
  <a href="/puzzles" class="flex items-center gap-3 px-4 py-3 w-full text-base text-text hover:bg-bg no-underline font-[DM_Sans]">
    <svg width="20" height="20" class="text-muted" aria-hidden="true"><use href="/sprites.svg#icon-archive"/></svg>
    Archive
  </a>
  <button data-fb-btn class="flex items-center gap-3 px-4 py-3 w-full text-left text-base text-text hover:bg-bg font-[DM_Sans]" type="button">
    <svg width="20" height="20" class="text-muted" aria-hidden="true"><use href="/sprites.svg#icon-feedback"/></svg>
    Feedback
  </button>
  <button data-htp-btn class="flex items-center gap-3 px-4 py-3 w-full text-left text-base text-text hover:bg-bg font-[DM_Sans]" type="button">
    <svg width="20" height="20" class="text-muted" aria-hidden="true"><use href="/sprites.svg#icon-help"/></svg>
    How to play
  </button>
  <button data-menu-close class="flex items-center gap-3 px-4 py-3 w-full text-left text-base text-muted hover:bg-bg font-[DM_Sans]" type="button" aria-label="Close menu">
    <svg width="20" height="20" aria-hidden="true"><use href="/sprites.svg#icon-circle-x"/></svg>
    Close
  </button>
</div>

<!-- ── Scrollable content ── -->
<div data-game-content class="flex-1 overflow-y-auto px-4 pt-8 pb-24">
  <div class="w-full max-w-sm mx-auto flex flex-col">

    <!-- Clue list (skeletons replaced by renderClues) -->
    <div data-clue-list role="list" aria-label="Puzzle clues" aria-busy="true"
         class="[display:grid] [grid-template-columns:max-content_1fr] gap-x-2 gap-y-4">
      <div class="clue-skeleton contents" aria-hidden="true">
        <div class="w-16 h-8 rounded bg-surface animate-pulse"></div>
        <div class="flex flex-col gap-2">
          <div class="h-4 w-4/5 rounded bg-surface animate-pulse"></div>
          <div class="h-4 w-2/5 rounded bg-surface animate-pulse"></div>
        </div>
      </div>
      <div class="clue-skeleton contents" aria-hidden="true">
        <div class="w-16 h-8 rounded bg-surface animate-pulse"></div>
        <div class="flex flex-col gap-2">
          <div class="h-4 w-4/5 rounded bg-surface animate-pulse"></div>
          <div class="h-4 w-2/5 rounded bg-surface animate-pulse"></div>
        </div>
      </div>
      <div class="clue-skeleton contents" aria-hidden="true">
        <div class="w-16 h-8 rounded bg-surface animate-pulse"></div>
        <div class="flex flex-col gap-2">
          <div class="h-4 w-4/5 rounded bg-surface animate-pulse"></div>
          <div class="h-4 w-2/5 rounded bg-surface animate-pulse"></div>
        </div>
      </div>
    </div>

    <!-- Digit boxes -->
    <div data-digits class="flex gap-2 w-full mt-2 h-28" role="group" aria-label="Digit entry boxes">
      <div class="flex-1 flex items-center justify-center bg-surface border-[1.5px] border-border rounded shadow-[3px_3px_0_rgba(38,38,36,0.12)] dark:shadow-[3px_3px_0_rgba(0,0,0,0.25)] cursor-pointer" data-digit="0" role="button" tabindex="0" aria-label="Hundreds digit" aria-expanded="false"></div>
      <div class="flex-1 flex items-center justify-center bg-surface border-[1.5px] border-border rounded shadow-[3px_3px_0_rgba(38,38,36,0.12)] dark:shadow-[3px_3px_0_rgba(0,0,0,0.25)] cursor-pointer" data-digit="1" role="button" tabindex="0" aria-label="Tens digit" aria-expanded="false"></div>
      <div class="flex-1 flex items-center justify-center bg-surface border-[1.5px] border-border rounded shadow-[3px_3px_0_rgba(38,38,36,0.12)] dark:shadow-[3px_3px_0_rgba(0,0,0,0.25)] cursor-pointer" data-digit="2" role="button" tabindex="0" aria-label="Units digit" aria-expanded="false"></div>
    </div>

    <!-- Keypad -->
    <div data-keypad-wrap class="w-full mt-2 hidden">
      <div data-keypad class="grid grid-cols-5 gap-2 w-full"></div>
    </div>

    <!-- Submit -->
    <div data-submit-wrap class="w-full mt-4 hidden">
      <button data-submit class="w-full h-14 rounded-sm bg-accent text-white text-base font-bold tracking-wide font-[DM_Sans] shadow-[3px_3px_0_rgba(38,38,36,0.18)] active:translate-x-[2px] active:translate-y-[2px] active:shadow-none" type="button">Check my guess</button>
    </div>

    <!-- Save score row -->
    <div data-save class="hidden items-center gap-1 mt-4 text-base text-muted font-[DM_Sans]">
      <input type="checkbox" checked id="cw-ck" data-save-check class="sr-only" />
      <label for="cw-ck" class="flex items-center gap-1 cursor-pointer">
        <svg class="ck-on" width="16" height="16" aria-hidden="true"><use href="/sprites.svg#icon-check"/></svg>
        <svg class="ck-off hidden" width="16" height="16" aria-hidden="true"><use href="/sprites.svg#icon-uncheck"/></svg>
        Keep my score
      </label>
    </div>

    <!-- Feedback -->
    <p data-feedback class="text-base font-bold leading-snug mt-4 hidden font-[DM_Sans]" aria-live="assertive"></p>

    <!-- Guess history -->
    <div data-history class="hidden mt-4">
      <p class="font-mono text-base font-bold uppercase tracking-widest text-muted mb-2">Your guesses</p>
      <ul data-history-list class="flex flex-wrap gap-2 list-none p-0 m-0"></ul>
    </div>

    <!-- Stats -->
    <div data-stats class="hidden mt-4"></div>

    <!-- Next puzzle -->
    <p data-next class="hidden mt-4 text-base text-muted font-[DM_Sans]">
      Puzzle <span data-next-number></span> is available tomorrow.
    </p>

    <!-- Random again -->
    <p data-again class="hidden mt-4 text-base font-[DM_Sans]">
      <a href="/random" class="text-accent underline">Play another random puzzle</a>
    </p>

  </div>
</div>
```

Key points:
- Add `bg-bg` to the game section's class list (per D-10, the game screen should have its own background)
- Data attributes match the existing `dom` cache in `app.ts`: `data-plabel`, `data-digits`, `data-digit="0|1|2"`, `data-keypad-wrap`, `data-keypad`, `data-submit-wrap`, `data-submit`, `data-save`, `data-save-check`, `data-feedback`, `data-history`, `data-history-list`, `data-stats`, `data-next`, `data-next-number`, `data-again`
- New data attributes for the menu: `data-game-header`, `data-menu-btn`, `data-menu`, `data-theme-toggle`, `data-theme-label`, `data-fb-btn`, `data-htp-btn`, `data-menu-close`, `data-game-content`
- The `data-clue-list` attribute replaces the old `.clue-list` class selector
- No `data-hint` element (per D-13, hint text is dropped)
- Skeleton loaders use `contents` display and `animate-pulse` (per D-03 and UI-SPEC)
- The submit button visible state uses plain `hidden` class toggled by JS (the old `visible` class toggle will be updated in Plan 02)
- The save row checkbox uses `sr-only` to hide the native checkbox; the label handles the visual toggle
- Per D-02, no card wrapper div around the clue list — clues render directly on bg-bg
  </action>
  <verify>
    <automated>cd /Users/jamiepersonal/Developer/clumeral-game && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` contains `data-game-header`
    - `index.html` contains `data-menu-btn`
    - `index.html` contains `id="game-menu"` and `data-menu`
    - `index.html` contains `data-clue-list`
    - `index.html` contains `data-theme-toggle` inside the menu (not in old footer)
    - `index.html` contains `data-fb-btn` inside the menu
    - `index.html` contains `data-htp-btn` inside the menu
    - `index.html` contains `data-menu-close`
    - `index.html` does NOT contain `id="puzzle"` (old card removed)
    - `index.html` does NOT contain `class="footer"` (old footer removed per D-11)
    - `index.html` does NOT contain `data-hint` (per D-13)
    - `index.html` does NOT contain `data-swatches` (colour picker removed)
    - `index.html` does NOT contain `fb-header` (feedback header button removed)
    - `index.html` does NOT contain `class="subtitle"` (subtitle removed — now on welcome screen)
    - `index.html` contains three `clue-skeleton` divs with `animate-pulse`
    - `index.html` contains three `data-digit` elements (0, 1, 2) inside the game screen section
    - `index.html` contains `Check my guess` as the submit button label
    - `public/sprites.svg` contains `id="icon-menu"`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Complete game screen HTML is in [data-screen="game"]. Old puzzle card, old footer links, feedback header, and subtitle are removed. Hamburger icon exists in sprites.svg. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove screens.ts overlay guard for empty game section</name>
  <files>src/screens.ts</files>
  <read_first>
    - src/screens.ts (current file with the guard at lines 39-44)
  </read_first>
  <action>
In `src/screens.ts`, inside the `updateScreenDOM()` function, remove the comment block and the `if (overlay)` guard that checks for an empty game section. Specifically, remove these lines:

```typescript
  // While the game screen section is empty (Phase 3 will populate it),
  // hide the entire overlay so the old game UI underneath is visible and interactive.
  if (overlay) {
    const gameEmpty = !dom.game.children.length;
    overlay.classList.toggle("invisible", next === "game" && gameEmpty);
  }
```

Also remove the `const overlay` line that queries `[data-screens]`:
```typescript
  const overlay = document.querySelector("[data-screens]") as HTMLElement | null;
```

The game section now has content, so this guard is obsolete. The overlay should always be visible when a screen is active.
  </action>
  <verify>
    <automated>cd /Users/jamiepersonal/Developer/clumeral-game && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens.ts` does NOT contain `gameEmpty`
    - `src/screens.ts` does NOT contain `invisible`
    - `src/screens.ts` does NOT contain `data-screens`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The screens.ts overlay guard is removed. The screen overlay is always visible when any screen is active. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without TypeScript errors
- `index.html` has complete game screen HTML inside `[data-screen="game"]` with all required data attributes
- Old puzzle card (`id="puzzle"`), old footer links, feedback header, and subtitle are gone from `index.html`
- `public/sprites.svg` has the `icon-menu` symbol
- `src/screens.ts` no longer has the empty-game-section guard
- No duplicate data attributes between old and new markup (because old is removed)
</verification>

<success_criteria>
The game screen section in `index.html` contains all the HTML elements needed for the game (header, menu, clues, digits, keypad, submit, save, feedback, history, stats, next, again) with correct Tailwind classes and data attributes. Old markup is fully removed. The build passes cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-screen-menu/03-01-SUMMARY.md`
</output>
