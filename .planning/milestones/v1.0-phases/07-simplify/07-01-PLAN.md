---
phase: 07-simplify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/octo.ts
  - src/tailwind.css
  - public/sprites.svg
  - index.html
  - src/app.ts
autonomous: false
requirements:
  - SIMP-01
must_haves:
  truths:
    - "No data-tlt references in CSS, HTML, or JS"
    - "No dead duplicate welcome h1 in index.html"
    - "Unused sprite icons removed from sprites.svg"
    - "All CSS rules in tailwind.css map to actually-used selectors"
    - "Each remaining component class in tailwind.css has a one-line comment explaining why it's not a utility"
    - "npm run build succeeds, no TypeScript errors"
  artifacts:
    - path: "src/octo.ts"
      provides: "revealLetters, tlts array, letter reveal timing all deleted"
    - path: "src/tailwind.css"
      provides: "No [data-tlt] rules, no unused selectors, comments on remaining component classes"
    - path: "public/sprites.svg"
      provides: "Only icons actually referenced in code"
---

<objective>
Remove dead code, consolidate duplicated markup, prune unused CSS rules, and add brief docs explaining remaining component CSS. Goal: smaller surface area before further design iteration.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Delete letter-reveal system (data-tlt)</name>
  <files>src/octo.ts, src/tailwind.css</files>
  <action>
Logo SVG was replaced by plain text h1. data-tlt letter reveal animation has nothing to animate.

**src/octo.ts:**
- Delete `const tlts = [...document.querySelectorAll('[data-tlt]')] as HTMLElement[];` (line 10)
- Delete entire `revealLetters` function (line 196+)
- Remove `revealLetters(() => setTimeout(...` call (around line 234), replace with just the setTimeout call

**src/tailwind.css:**
- Delete the `[data-tlt]` rule block (around line 145, "Title letter groups" comment + rule)
  </action>
  <acceptance_criteria>
    - grep "data-tlt" src/ returns nothing
    - grep "revealLetters" src/ returns nothing
    - grep "tlts" src/octo.ts returns nothing
    - npm run build passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Delete duplicate welcome h1 in index.html</name>
  <files>index.html</files>
  <action>
Welcome screen content is fully rendered by `renderWelcome()` in welcome.ts via `screen.innerHTML = ...`. The static markup inside `<section data-screen="welcome">` in index.html is dead — overwritten on every render.

- Find the `<section data-screen="welcome">` block in index.html
- Remove all child content between opening and closing tags. Leave the shell:
```html
<section data-screen="welcome" class="absolute inset-0 ..." aria-hidden="false"></section>
```
- Keep the section element itself (screens.ts references it)
  </action>
  <acceptance_criteria>
    - `[data-screen="welcome"]` in index.html is empty (no inner markup)
    - Welcome screen still renders correctly (content injected by welcome.ts)
    - npm run build passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Remove unused sprite icons</name>
  <files>public/sprites.svg</files>
  <action>
Confirmed unused in code via grep: `icon-menu`, `icon-sun`, `icon-github`, `icon-heart`.

- Read public/sprites.svg
- Remove the `<symbol id="icon-menu">...</symbol>` block
- Remove the `<symbol id="icon-sun">...</symbol>` block
- Remove the `<symbol id="icon-github">...</symbol>` block
- Remove the `<symbol id="icon-heart">...</symbol>` block
- Keep all other symbols (they're referenced)
  </action>
  <acceptance_criteria>
    - grep "icon-menu\|icon-sun\|icon-github\|icon-heart" public/sprites.svg returns nothing
    - All still-used icons (check, uncheck, cookie, info, archive, feedback, help, moon, circle-x) remain in sprites.svg
    - npm run build passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Audit + document remaining custom CSS in tailwind.css</name>
  <files>src/tailwind.css</files>
  <action>
For each custom class block in tailwind.css, verify it's used and add a brief comment if not already documented.

Verification command for each class name:
```
grep -rE "className?=[\"'][^\"']*\bCLASS\b|\\.CLASS\\b|querySelector.*\bCLASS\b" src/ index.html
```

**Check and either DELETE (if unused) or ADD COMMENT (if used, explaining why it's not a utility):**

- `.skip-link` — used (index.html), keep. Comment: "Positioned off-screen until focus — Tailwind's sr-only doesn't re-show on focus."
- `.burger-btn`, `.burger-bar` — used. Comment already present.
- `[data-save]` visibility — used (index.html + CSS sibling selector). Keep comment.
- `#cw-ck` toggle rules — used (save checkbox). Keep existing comments.
- `.htp-clue`, `.clue__*`, `.htp-*` — used by welcome.ts HTP markup. Add grouping comment: "HTP screen BEM classes — kept because the markup is dense grid-based and utilities would balloon the HTML."
- `.digit-box`, `.digit-box__*`, `.elim` — used by both HTP (welcome.ts) and game (app.ts renderBox). Keep. Comment: "Shared digit-box component — 3/4/3 grid with container-query font sizing, used by HTP demo and live game."
- `.recurring` — check usage. If unused, delete. If used, keep with comment.
- `.toast-msg` — check usage in modals.ts. If unused, delete.
- `.warn` — check usage. If unused, delete.
- `[data-digits].digit-correct` — used by octo.ts on solve. Keep.
- `[data-octo-wrap]`, `[data-octo]` rules — used by octo.ts. Keep.
- `@keyframes octo-fly`, `octo-colours` — used. Keep.
- `[data-fb-modal]` rules — used. Keep.

For any class confirmed UNUSED, delete the rule block.

Add a top-of-file comment summary listing which Tailwind overrides are applied via @theme (tokens, fonts) vs which component classes live in this file and why.
  </action>
  <acceptance_criteria>
    - Every custom class in tailwind.css is either deleted (unused) or has an explanatory comment
    - File has a top comment block listing component class groups
    - npm run build passes, no visual regressions introduced
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Visual regression check</name>
  <what-built>
Dead code removed (letter reveal, duplicate h1, unused icons). Custom CSS in tailwind.css audited — unused rules deleted, remaining rules documented. No behaviour changes expected.
  </what-built>
  <how-to-verify>
Dev server should already be running. In browser:

1. Welcome screen — octo + "Clumeral" logo (Comfortaa bold) + subtitle + Play button + HTP (clue tag, digit boxes, worked example) all render
2. Game screen (Play) — clues with tags, digit boxes 3x 3/4/3 grid, keypad opens on tap, submit button
3. Save score checkbox appears when submit visible, toggles SVG tick/untick with cookie icon
4. Dark mode toggle via menu — all colours swap correctly
5. Tooltip on clue tag — light grey box, X close button, flip above/below based on viewport
6. Burger → X animation on menu open
7. Celebration animation on correct answer → completion screen with stats
8. Feedback modal — category pills, textarea, send button

Type "approved" if everything looks correct, or describe any visual regressions.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
After all tasks:
1. `npm run build` succeeds
2. `npx tsc --noEmit` exits 0
3. No grep hits for removed identifiers (data-tlt, revealLetters, icon-menu, icon-sun, icon-github, icon-heart)
4. User visually confirmed no regressions
</verification>

<success_criteria>
- Dead code removed (letter reveal, duplicate markup, unused icons)
- Custom CSS audited — unused rules deleted, remaining rules have comments
- Build passes
- User approved visual state
</success_criteria>

<output>
Create `.planning/phases/07-simplify/07-01-SUMMARY.md` after completion.
</output>
