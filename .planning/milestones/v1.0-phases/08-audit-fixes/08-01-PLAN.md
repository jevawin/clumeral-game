---
phase: 08-audit-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/app.ts
  - src/modals.ts
autonomous: true
requirements: [GAM-01, GAM-06, FBK-01]

must_haves:
  truths:
    - "Game header renders a puzzle-number/date label for daily puzzles (e.g. 'Puzzle #N · <date>')"
    - "Game header renders 'Random puzzle' label for the /random flow"
    - "Game header renders 'Archived puzzle' label plus puzzle number/date for /puzzles/:n replay flow"
    - "Visiting /puzzles/:n lands directly on the game screen (no welcome-screen flash)"
    - "modals.ts no longer references the dead [data-fb-header-btn] selector"
    - "All seven E2E flows from the v1.0 milestone audit pass end-to-end: (1) Welcome → Game (Play btn), (2) Game → Celebration → Completion (correct guess + callback), (3) Game menu → Feedback modal, (4) Completion → Feedback modal, (5) Reduced-motion path skips celebration, (6) Random puzzle (/random), (7) Replay puzzle (/puzzles/:n) lands directly on game and shows the puzzle label"
  artifacts:
    - path: "index.html"
      provides: "[data-plabel] span inside [data-game-header] left flex container"
      contains: "data-plabel"
    - path: "src/app.ts"
      provides: "Module-scope replayMatch detection used by initScreens + initWelcome gate"
      contains: "replayMatch"
    - path: "src/modals.ts"
      provides: "Feedback modal wiring with no stale [data-fb-header-btn] reference"
      contains: "data-fb-btn"
  key_links:
    - from: "src/app.ts startDailyPuzzle/startRandomPuzzle/startReplayPuzzle"
      to: "index.html [data-game-header] [data-plabel]"
      via: "dom.plabel cache populated at app.ts:58"
      pattern: "data-plabel"
    - from: "src/app.ts init block (path detection)"
      to: "initScreens('game') for replay path"
      via: "replayMatch regex /^/puzzles/(\\d+)$/"
      pattern: "isRandomPath \\|\\| !!replayMatch"
---

<objective>
Close the three v1.0 audit gaps: missing `[data-plabel]` element (GAM-01, GAM-06), `/puzzles/:n` replay path landing on welcome screen (GAM-06), and stale `[data-fb-header-btn]` query in modals.ts (FBK-01).

Purpose: Ship v1.0-clean — the game header shows puzzle identity for every flow, archive links land players directly in the game, and dead selectors stop wasting executor attention.

Output: `index.html` with the new label element, `src/app.ts` with replay-aware screen init, `src/modals.ts` with the dead query removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@docs/CONVENTIONS.md
@index.html
@src/app.ts
@src/modals.ts

<interfaces>
<!-- Pre-existing wiring the executor must NOT change -->

From src/app.ts:58 — DOM cache already includes plabel:
```typescript
plabel: $('[data-plabel]') as HTMLElement | null,
```

Three sites already write to dom.plabel (no change to these — they start working once the element exists):
- src/app.ts:546 — startRandomPuzzle: `if (dom.plabel) dom.plabel.textContent = "Random puzzle";`
- src/app.ts:558 — startDailyPuzzle: `if (dom.plabel) dom.plabel.textContent = `Puzzle #${num} · ${formatDate(date)}`;`
- src/app.ts:580–585 — startReplayPuzzle: prepends an "Archived puzzle" sibling via `dom.plabel.parentElement?.insertBefore(label, dom.plabel)`, then sets `dom.plabel.textContent`.

From src/app.ts:705 — replay regex already used inside loadPuzzle():
```typescript
const replayMatch = path.match(/^\/puzzles\/(\d+)$/);
```
This regex must be reused at module scope so the init block (lines 889–891) can consult it.

From index.html:144–153 — game header left section is a flex container with the wordmark span as last child. The new `[data-plabel]` span must be inserted as a SIBLING of the wordmark span, inside the same flex container, so `parentElement?.insertBefore` in startReplayPuzzle works.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add [data-plabel] span to game header (GAM-01, GAM-06)</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 130–160 (confirm flex container at line 145 ends with the wordmark span at line 152, and the container's `</div>` close is on line 153)
    - src/app.ts:580–585 (confirm startReplayPuzzle uses dom.plabel.parentElement?.insertBefore — the new element must be a direct child of the flex container)
  </read_first>
  <action>
    In `index.html`, inside `<header data-game-header>` (line 144), inside the `<div class="flex items-center gap-2">` left container (lines 145–153), insert a new span as the LAST CHILD of that flex div — immediately AFTER the existing `<span class="text-xl font-bold ...">Clumeral</span>` on line 152, and immediately BEFORE the `</div>` on line 153.

    The new span MUST be a direct child of the `<div class="flex items-center gap-2">` so that at runtime `dom.plabel.parentElement` resolves to that flex div. This is required for `startReplayPuzzle` (src/app.ts:580–585) to correctly insert the "Archived puzzle" sibling label via `parentElement?.insertBefore(label, dom.plabel)`.

    Insert this exact line, keeping 2-space indentation aligned with siblings (i.e. matching the indentation of the Clumeral wordmark span on line 152):

    ```html
            <span data-plabel class="ml-2 text-sm text-muted font-[Quicksand]"></span>
    ```

    Do NOT change any other markup. Do NOT touch the burger button on line 154 (post-insertion: line 155). Preserve the surrounding `<!-- ── Game header ── -->` comment on line 143.

    Why these classes: `text-sm text-muted font-[Quicksand]` matches the muted Quicksand label style already used elsewhere (audit hint). `ml-2` adds breathing room from the wordmark. Empty content is intentional — `startDailyPuzzle`, `startRandomPuzzle`, and `startReplayPuzzle` populate it at runtime.
  </action>
  <verify>
    <automated>grep -q 'data-plabel class="ml-2 text-sm text-muted font-\[Quicksand\]"' index.html && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'data-plabel' index.html` returns exactly one line
    - The matched line sits between the Clumeral wordmark span and the closing `</div>` of the `flex items-center gap-2` container (i.e. it is a direct child of that flex div, not a sibling of it)
    - The matched line contains the substring `class="ml-2 text-sm text-muted font-[Quicksand]"`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    - `[data-plabel]` element exists as a sibling of the Clumeral wordmark span inside the game header flex container
    - `dom.plabel` cache at `src/app.ts:58` now resolves to a real element
    - `dom.plabel.parentElement` resolves to the `<div class="flex items-center gap-2">` so startReplayPuzzle's `insertBefore` works
    - The three start* functions populate the header label at runtime (no code changes needed there)
  </done>
</task>

<task type="auto">
  <name>Task 2: Route /puzzles/:n replay path to game screen (GAM-06)</name>
  <files>src/app.ts</files>
  <read_first>
    - src/app.ts:700–730 (loadPuzzle — already computes `replayMatch` at line 705)
    - src/app.ts:880–895 (init block — currently only checks `isRandomPath`)
  </read_first>
  <action>
    Edit `src/app.ts` lines 889–891 only. Current code:

    ```typescript
    const isRandomPath = window.location.pathname === '/random';
    initScreens(isRandomPath ? 'game' : 'welcome');
    if (!isRandomPath) initWelcome();
    ```

    Replace with:

    ```typescript
    const isRandomPath = window.location.pathname === '/random';
    const replayMatch = window.location.pathname.match(/^\/puzzles\/(\d+)$/);
    initScreens((isRandomPath || !!replayMatch) ? 'game' : 'welcome');
    if (!isRandomPath && !replayMatch) initWelcome();
    ```

    Notes:
    - Use the exact same regex `/^\/puzzles\/(\d+)$/` as line 705 — do NOT broaden or narrow it.
    - The local `replayMatch` inside `loadPuzzle()` at line 705 stays untouched. We are creating a separate module-scope const because `loadPuzzle()` is async and returns before `initScreens` would otherwise have a chance to read its local.
    - Preserve Prettier 200-col formatting and the surrounding `// ─── Init ───` section header.
    - Do NOT touch lines 885–888 (initTheme, initMenu, initFeedbackModal, loadPuzzle calls).
  </action>
  <verify>
    <automated>grep -nE "const replayMatch = window\.location\.pathname\.match" src/app.ts && grep -nE "isRandomPath \|\| !!replayMatch" src/app.ts && grep -nE "!isRandomPath && !replayMatch" src/app.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "const replayMatch = window.location.pathname.match" src/app.ts` returns exactly one match in the init block (line number > 880, < 900)
    - `grep -n "isRandomPath || !!replayMatch" src/app.ts` returns exactly one match
    - `grep -n "!isRandomPath && !replayMatch" src/app.ts` returns exactly one match
    - The regex `/^\/puzzles\/(\d+)$/` still appears at exactly two locations: inside `loadPuzzle()` (~line 705) and the new module-scope line
    - `npm run build` exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>
    - Visiting `/puzzles/:n` initialises directly on the game screen
    - `initWelcome()` is skipped for replay paths (avoids unneeded welcome-screen DOM work)
    - `/random` and daily-puzzle behaviour unchanged
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove stale [data-fb-header-btn] query and listener (FBK-01)</name>
  <files>src/modals.ts</files>
  <read_first>
    - src/modals.ts lines 25–60 (modal element queries — confirms `headerBtn` is declared at line 39 alongside live queries)
    - src/modals.ts line 138 (the `if (headerBtn) headerBtn.addEventListener("click", openFeedback);` usage — must be removed too)
  </read_first>
  <action>
    Two deletions in `src/modals.ts`:

    1. Delete line 39 in full:
    ```typescript
      const headerBtn = document.querySelector('[data-fb-header-btn]');
    ```

    2. Delete line 138 in full:
    ```typescript
      if (headerBtn) headerBtn.addEventListener("click", openFeedback);
    ```

    Do NOT touch line 40 (`const footerBtn = document.querySelector('[data-fb-btn]');`) — that selector is live.
    Do NOT touch line 38 (`sendBtn`).
    Do NOT touch any other listener wiring.

    After both deletions, `headerBtn` must not appear anywhere in the file. Preserve Prettier formatting and any `// ─── Section ───` headers above and below the deleted lines.
  </action>
  <verify>
    <automated>! grep -q 'data-fb-header-btn' src/modals.ts && ! grep -q 'headerBtn' src/modals.ts && grep -q "data-fb-btn'" src/modals.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-fb-header-btn' src/modals.ts` returns 0
    - `grep -c 'headerBtn' src/modals.ts` returns 0
    - `grep -c "data-fb-btn'" src/modals.ts` returns at least 1 (footerBtn query untouched)
    - `npm run build` exits 0 with no TypeScript "unused variable" or "undefined name" errors
  </acceptance_criteria>
  <done>
    - No reference to `data-fb-header-btn` or `headerBtn` remains in `src/modals.ts`
    - Feedback modal continues to open from the game menu and from the completion screen (verified at phase level)
    - Tech-debt item from audit (modals.ts:39 stale query) closed
  </done>
</task>

</tasks>

<verification>
After all three tasks, confirm the seven E2E flows from `.planning/v1.0-MILESTONE-AUDIT.md`:

1. Build clean: `npm run build` exits 0.
2. **Flow 1 — Welcome → Game (Play btn):** load `/`, click Play, game screen renders with `Puzzle #N · <date>` in the header.
3. **Flow 2 — Game → Celebration → Completion:** submit a correct guess; celebration plays then completion screen renders.
4. **Flow 3 — Game menu → Feedback modal:** open game menu, click feedback trigger, modal opens with no console errors.
5. **Flow 4 — Completion → Feedback modal:** from the completion screen, the feedback prompt opens the modal with no console errors.
6. **Flow 5 — Reduced-motion path skips celebration:** with `prefers-reduced-motion: reduce`, a correct guess routes to completion without the celebration animation.
7. **Flow 6 — Random puzzle (`/random`):** init goes straight to game; header shows `Random puzzle`.
8. **Flow 7 — Replay puzzle (`/puzzles/3`):** page loads directly into the game screen (no welcome flash); header shows the archived-puzzle sibling label PLUS `Puzzle #3 · <date>` in the `[data-plabel]` span.
9. Source check: `grep -r 'data-fb-header-btn' src/` returns no matches.
</verification>

<success_criteria>
- [ ] `[data-plabel]` element exists in `index.html` inside the game header left flex container
- [ ] `src/app.ts` init block detects `replayMatch` at module scope and routes both `/random` and `/puzzles/:n` to the game screen
- [ ] `initWelcome()` is gated on `!isRandomPath && !replayMatch`
- [ ] `src/modals.ts` no longer contains `data-fb-header-btn` or `headerBtn`
- [ ] `npm run build` exits 0
- [ ] All seven E2E flows from audit pass end-to-end (see `<verification>` block)
</success_criteria>

<output>
After completion, create `.planning/phases/08-audit-fixes/08-01-SUMMARY.md` documenting:
- The three diffs applied (one per task)
- Confirmation that GAM-01, GAM-06, FBK-01 are now satisfied
- Any deviations from this plan
</output>
</content>
</invoke>