---
phase: 05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/app.ts
  - src/welcome.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "app.ts and welcome.ts both import date helpers from src/date.ts — neither defines its own todayLocal/puzzleNumber/formatDate/EPOCH_DATE any more (covers D-03)"
    - "All client date keying flows through the single src/date.ts module, so the client always uses the LOCAL date as source of truth (covers D-01, D-02)"
    - "The duplicate local helper definitions are deleted (not merely shadowed) so the imported helpers actually take effect"
    - "The app still builds and the full vitest suite shows no new failures"
  artifacts:
    - path: "src/app.ts"
      provides: "Game controller now consuming canonical date helpers"
      contains: "from './date.ts'"
    - path: "src/welcome.ts"
      provides: "Welcome screen now consuming canonical date helpers + formatDate for display"
      contains: "from './date.ts'"
  key_links:
    - from: "src/app.ts"
      to: "src/date.ts"
      via: "import { todayKey, puzzleNumberFor, formatDate, EPOCH_DATE }"
      pattern: "from './date.ts'"
    - from: "src/welcome.ts"
      to: "src/date.ts"
      via: "import { todayKey, puzzleNumberFor, formatDate }"
      pattern: "from './date.ts'"
---

<objective>
Rewire `src/app.ts` and `src/welcome.ts` to consume the canonical `src/date.ts` module from Plan 01, and DELETE their duplicated local date helpers and `EPOCH_DATE` constants. This is the de-duplication step that actually makes D-03 true at runtime: until the local copies are removed, a same-named local function silently shadows the import and the bug persists.

Purpose: Collapse the duplicated client date logic into the single source (D-03), so the client's local date is the one source of truth (D-01, D-02). This directly heals the #205 / #209 root cause on the client side.
Output: app.ts and welcome.ts import from date.ts; no duplicate helpers remain; build green; suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-CONTEXT.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-RESEARCH.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-PATTERNS.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-01-SUMMARY.md

<interfaces>
src/date.ts exports (confirm exact names against 05-01-SUMMARY.md before importing):

```typescript
export const EPOCH_DATE: string;
export function localDateKey(d: Date): string;
export function todayKey(): string;            // was todayLocal()
export function puzzleNumberFor(dateStr): number; // was puzzleNumber()
export function formatDate(dateStr): string;
```

Call-site rename map for this plan:
- existing local `todayLocal()`  -> imported `todayKey()`
- existing local `puzzleNumber(x)` -> imported `puzzleNumberFor(x)`
- existing local `formatDate(x)` -> imported `formatDate(x)` (same name)
- existing local `EPOCH_DATE`    -> imported `EPOCH_DATE`
</interfaces>

<source_excerpts>
VERIFIED locations to edit (from RESEARCH.md + PATTERNS.md):

app.ts:
- EPOCH_DATE constant defined at ~L51 — DELETE.
- "Date helpers" section ~L96-114 (todayLocal, puzzleNumber, formatDate) — DELETE the whole section.
- Existing import block ~L4-14 — EXTEND with the date.ts import.
- Internal callers to rename: todayEntry() (~L118-121) calls todayLocal; replay path date->puzzleNumber (~L900-920); any puzzleNumber()/formatDate() call site.

welcome.ts:
- EPOCH_DATE at ~L13 — DELETE.
- todayLocal() ~L15-18 and puzzleNumber() ~L20-23 — DELETE.
- Inline toLocaleDateString at ~L131 — REPLACE with formatDate(today).
- Existing import (navigate from './router.ts') ~L6 — add date.ts import below it.
</source_excerpts>

<pitfall_warning>
PITFALL 3 (RESEARCH.md): TypeScript lets a module-level function shadow an import of the same name with NO compiler error — the old local function wins. You MUST delete every local definition (todayLocal, puzzleNumber, formatDate, EPOCH_DATE) in BOTH files. After editing, grep to prove no local `function todayLocal` / `function puzzleNumber` / local `EPOCH_DATE =` remains. Renaming the import call sites to todayKey/puzzleNumberFor avoids accidental shadowing entirely for those two — but formatDate keeps its name, so the local formatDate MUST be physically removed.
</pitfall_warning>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire src/app.ts to date.ts and delete duplicate helpers</name>
  <files>src/app.ts</files>
  <read_first>
    - src/app.ts (full date-helper region ~L4-14 imports, L51 EPOCH_DATE, L96-114 helpers, L118-121 todayEntry, L900-920 replay puzzleNumber call)
    - src/date.ts (the canonical exports to import)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-01-SUMMARY.md (final export names)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-PATTERNS.md (section "src/app.ts" — exact removal + import guidance)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-RESEARCH.md (Pitfall 3 — shadowing)
  </read_first>
  <action>
    In src/app.ts: (1) Add an import from './date.ts' bringing in EPOCH_DATE, todayKey, puzzleNumberFor, formatDate (only the names actually used — if EPOCH_DATE is unused after edits, omit it). (2) DELETE the standalone `const EPOCH_DATE = "2026-03-08";` at ~L51. (3) DELETE the entire "Date helpers" section ~L96-114 (the local todayLocal, puzzleNumber, formatDate). (4) Rename every internal caller: todayLocal() -> todayKey() (including inside todayEntry()), puzzleNumber(...) -> puzzleNumberFor(...), and keep formatDate(...) calls as-is (now resolving to the import). Do not change any other game logic, render functions, or the gameState shape. Keep the `.ts` import extension per CONVENTIONS. This makes D-01/D-02/D-03 true on the main game controller.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "function todayLocal|function puzzleNumber\\b|function formatDate" src/app.ts` returns NO matches (local helpers deleted).
    - `grep -c "EPOCH_DATE = " src/app.ts` returns 0 (local constant deleted; the imported one carries no `=` assignment).
    - `grep -c "from './date.ts'" src/app.ts` returns 1.
    - `grep -cE "\\btodayLocal\\(|\\bpuzzleNumber\\(" src/app.ts` returns 0 (all call sites renamed to todayKey/puzzleNumberFor).
    - `npm run build` exits 0 (TypeScript compiles; no unresolved/shadowed references).
  </acceptance_criteria>
  <done>app.ts imports date helpers from date.ts, all local duplicates removed, all call sites renamed, build green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire src/welcome.ts to date.ts and replace inline date format</name>
  <files>src/welcome.ts</files>
  <read_first>
    - src/welcome.ts (L6 import, L13 EPOCH_DATE, L15-23 helpers, L129-132 puzzle-number display + L131 inline toLocaleDateString)
    - src/date.ts (canonical exports)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-01-SUMMARY.md (final export names)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-PATTERNS.md (section "src/welcome.ts" — exact removal + inline-call replacement)
  </read_first>
  <action>
    In src/welcome.ts: (1) Add an import from './date.ts' for todayKey, puzzleNumberFor, formatDate (only the names used). (2) DELETE `const EPOCH_DATE = "2026-03-08";` at ~L13. (3) DELETE the local todayLocal() (~L15-18) and puzzleNumber() (~L20-23). (4) Rename call sites: todayLocal() -> todayKey(), puzzleNumber(...) -> puzzleNumberFor(...). (5) Replace the inline `new Date(today + "T00:00:00").toLocaleDateString("en-GB", {...})` at ~L131 with `formatDate(today)` so welcome and app share one display path (resolves Open Question 1 in RESEARCH.md — include it for consistency). Do not change the welcome screen layout, copy, or navigation. Keep `.ts` import extension. This finishes D-03 across the second client file.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "function todayLocal|function puzzleNumber\\b" src/welcome.ts` returns NO matches.
    - `grep -c "EPOCH_DATE = " src/welcome.ts` returns 0.
    - `grep -c "from './date.ts'" src/welcome.ts` returns 1.
    - `grep -c "toLocaleDateString" src/welcome.ts` returns 0 (inline format replaced by formatDate import).
    - `grep -cE "\\btodayLocal\\(|\\bpuzzleNumber\\(" src/welcome.ts` returns 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>welcome.ts imports from date.ts, local duplicates removed, inline date format replaced with formatDate, build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI -> date.ts | UI reads the local clock via the shared helper to pick the puzzle day. No new external input crosses here; this plan only consolidates existing trusted code paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | shadowed-import regression | mitigate | Grep acceptance gates prove the local duplicate functions/constants are physically removed, so the imported canonical helper cannot be silently shadowed (Pitfall 3). |
| T-05-04 | Repudiation | puzzleNumber drift across files | accept | Both files now derive puzzle number from one deterministic helper; cross-file disagreement is structurally impossible once duplicates are deleted. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH.md Package Legitimacy Audit: zero new packages this phase). |
</threat_model>

<verification>
- `npm run build` exits 0 after both edits.
- No local date helpers or EPOCH_DATE constants remain in app.ts or welcome.ts (grep gates above).
- `npx vitest run` shows no NEW failures vs the documented pre-existing router.spec.ts beacon failure.
</verification>

<success_criteria>
- app.ts and welcome.ts both import date helpers from src/date.ts (D-03).
- Zero duplicate todayLocal/puzzleNumber/formatDate/EPOCH_DATE definitions remain on the client (D-03).
- welcome's date display uses the shared formatDate.
- Client uses local date as source of truth everywhere (D-01, D-02).
</success_criteria>

<output>
Create `.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-02-SUMMARY.md` when done. Note any call sites found beyond the documented locations.
</output>
