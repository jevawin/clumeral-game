---
phase: 05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn
plan: 05
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-04
files_modified:
  - src/app.ts
  - src/completion.ts
  - tests/completion-stats.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "On reload mid-game, a daily-puzzle player resumes the exact board: eliminated digits per box, wrong guesses, active keypad box, and last feedback message are restored (covers D-06, fixes #206)"
    - "Mid-game state is saved on each digit elimination, after each wrong guess, and on active-box change (covers D-06)"
    - "Saved state is cleared on solve, and a stale (yesterday) state is never restored (covers D-07)"
    - "Restore happens ONLY for daily puzzles — random and archive-replay starts never save or restore (covers D-08)"
    - "computeStats streak counts consecutive local days correctly with no phantom gaps, stays alive when today is unplayed, breaks on a real gap, and ignores same-day duplicates — proven by tests (fixes #209)"
    - "Restore is invoked only AFTER renderClues so digit boxes have DOM to render into (Pitfall 2)"
  artifacts:
    - path: "src/app.ts"
      provides: "saveActive hooks (eliminate/guess/box), clearActive on solve, restore in startDailyPuzzle"
      contains: "loadActive"
    - path: "src/completion.ts"
      provides: "streak walk confirmed correct with local-keyed history (no code change needed unless a bug is found); countdown stays local-midnight"
    - path: "tests/completion-stats.spec.ts"
      provides: "Streak scenario tests (today unplayed, real gap, same-day dup, consecutive run)"
      min_lines: 30
  key_links:
    - from: "src/app.ts startDailyPuzzle"
      to: "src/storage.ts loadActive"
      via: "restore after renderClues, before gameState reset, daily-only"
      pattern: "loadActive"
    - from: "src/app.ts handleGuess (correct branch)"
      to: "src/storage.ts clearActive"
      via: "clearActive() after recordGame, before renderCompletion/replaceRoute"
      pattern: "clearActive"
---

<objective>
Wire mid-game persistence into the game flow and lock down the streak fix. Add `saveActive` hooks on the three board-mutating actions, `clearActive` on solve, and a restore step inside `startDailyPuzzle` (daily-only, after clues render). Then verify/finish #209: confirm `computeStats` counts consecutive local days correctly now that all dates flow through `date.ts`, and prove it with a streak test suite. This is the plan that makes #206 and #209 observable to the player.

Purpose: Implement D-06 (save + restore full board/feedback), D-07 (clear on solve + no stale restore), D-08 (daily-only), and verify the #209 streak heal with tests.
Output: app.ts hooks + restore, completion.ts streak verified, tests/completion-stats.spec.ts green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-CONTEXT.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-RESEARCH.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-PATTERNS.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-VALIDATION.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-02-SUMMARY.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-04-SUMMARY.md

<interfaces>
Consumes from src/storage.ts (Plan 04 — confirm exact names in 05-04-SUMMARY.md):
```typescript
import { saveActive, loadActive, clearActive } from './storage.ts';
import type { ActiveState } from './types.ts';
```
ActiveState fields: { v:1; date; possibles:number[][]; guesses:number[]; activeBox:number|null; feedbackKey:string|null }.

Consumes from src/date.ts (Plan 02 already imports it into app.ts): todayKey().

Existing app.ts renderers to reuse for restore (idempotent, render-from-state):
- renderAllBoxes() / renderBox — redraw possibles
- renderHistory(gameState.guesses) — redraw wrong guesses
- openBox(i) — open a keypad box
- renderFeedback("incorrect") — show last feedback
</interfaces>

<source_excerpts>
VERIFIED hook locations (RESEARCH.md + PATTERNS.md). Line numbers are pre-Plan-02; after Plan 02's deletions they shift up ~18 lines — locate by function name, not raw line:

- Save on wrong guess: in handleGuess, after `gameState.guesses.push(guess)` (incorrect branch) — call saveActive(buildActiveState()) after the push, before renderFeedback.
- Save on digit elimination: in toggleDigit, after possibles is mutated and before/after renderBox+checkSubmit — call saveActive(buildActiveState()).
- Save on active-box change: in openBox/selectBox, after activeBox is set — call saveActive(buildActiveState()).
- Clear on solve: in handleGuess correct branch, after `recordGame(...)`, before renderCompletion/replaceRoute — call clearActive().
- Restore: in startDailyPuzzle, AFTER renderClues(clues) (its first line) and AFTER the `if (entry) {...return;}` already-solved block, BEFORE `gameState = {...}; resetPuzzleUI();` — attempt loadActive().

Guard: ONLY save in daily mode. handleGuess already branches on gameState.isRandom for recordGame; gate saveActive the same way (no save when isRandom, and startReplayPuzzle / startRandomPuzzle must never call saveActive/loadActive — D-08). startDailyPuzzle is the only restore site.

computeStats: src/completion.ts ~L45-78 streak walk; parses `entry.date + 'T00:00:00'` (NO Z — local, correct, L59); day diff via Math.round(diff/86400000). formatCountdown ~L83-93 uses setHours(24,0,0,0) (local midnight — already correct).
</source_excerpts>

<pitfalls>
- Pitfall 2: restore MUST run after renderClues(clues) — boxes need DOM to render into, else silent no-op (blank boxes).
- Pitfall 4: staleness is enforced INSIDE loadActive (Plan 04) — app.ts does not re-check the date; just trust loadActive's null.
- Pitfall 5: saving on every toggle is fine (payload tiny) — no debounce.
- feedbackKey only ever "incorrect" | "error" | null (solve clears state, so never "correct").
- computeStats is module-PRIVATE in completion.ts. Per PATTERNS.md, test it INDIRECTLY via renderCompletion (seed localStorage dlng_history, call renderCompletion, assert the rendered streak stat) to avoid widening the module API. Only export it if indirect testing proves infeasible — document the choice.
</pitfalls>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire saveActive/clearActive/restore into src/app.ts (daily-only)</name>
  <files>src/app.ts</files>
  <read_first>
    - src/app.ts (handleGuess correct + incorrect branches; toggleDigit; openBox/selectBox; startDailyPuzzle; startRandomPuzzle; startReplayPuzzle; gameState shape; renderAllBoxes/renderHistory/openBox/renderFeedback signatures)
    - src/storage.ts (saveActive/loadActive/clearActive — Plan 04)
    - src/types.ts (ActiveState)
    - src/date.ts (todayKey — already imported by Plan 02)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-04-SUMMARY.md (exact storage fn + ActiveState field names)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-PATTERNS.md (section "src/app.ts" — exact hook insertion points + idempotent-restore pattern)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-RESEARCH.md (Pitfalls 2 + 4; restore step list)
  </read_first>
  <action>
    In src/app.ts: extend the existing './storage.ts' import to add saveActive, loadActive, clearActive (do not add a second import from the same module); add `import type { ActiveState } from './types.ts';` if not already importing the type. Add a small private helper buildActiveState(): ActiveState that snapshots current board state into the ActiveState shape — v:1, date: gameState.date (the daily date), possibles mapped to number[][] (Set -> Array per box), guesses: [...gameState.guesses], activeBox: the current activeBox value (number|null), feedbackKey: the last feedback key ("incorrect"|"error"|null). Add saveActive(buildActiveState()) calls at the three mutation sites: after the wrong-guess push in handleGuess (incorrect branch), after the possibles mutation in toggleDigit, and after activeBox is set in openBox/selectBox. Gate ALL saves to daily mode only — do not save when gameState.isRandom is true; startRandomPuzzle and startReplayPuzzle must never save or restore (D-08). Add clearActive() in handleGuess correct branch after recordGame(...) and before renderCompletion/replaceRoute (D-07). In startDailyPuzzle, AFTER renderClues(clues) and AFTER the already-solved `if (entry) { ...; return; }` block, attempt const draft = loadActive(); if draft is non-null: set gameState = { answer:null, guesses: draft.guesses, solved:false, puzzleNum: num, date }, rebuild possibles from draft.possibles (Array -> Set per box), call resetPuzzleUI() if needed then the idempotent renderers — renderAllBoxes(), renderHistory(gameState.guesses), if draft.activeBox !== null call openBox(draft.activeBox), if draft.feedbackKey === 'incorrect' call renderFeedback('incorrect') — then RETURN (skip the fresh-start branch). If draft is null, fall through to the existing fresh `gameState = {...}; resetPuzzleUI();` path unchanged. Reuse existing renderers only; write no new DOM logic (idempotent render-from-state pattern). This implements D-06, D-07, D-08.
  </action>
  <verify>
    <automated>npm run build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "saveActive(" src/app.ts` returns >= 3 (three mutation hooks).
    - `grep -c "clearActive()" src/app.ts` returns >= 1 (solve hook).
    - `grep -c "loadActive()" src/app.ts` returns >= 1 (restore in startDailyPuzzle).
    - `grep -c "saveActive" src/app.ts` shows NO occurrence inside startRandomPuzzle or startReplayPuzzle bodies (verify by reading those functions — D-08 daily-only).
    - The restore block appears AFTER the renderClues(clues) call within startDailyPuzzle (verify by reading; Pitfall 2).
    - `npm run build` exits 0 and `npx vitest run` shows no NEW failures vs the documented pre-existing router beacon failure.
  </acceptance_criteria>
  <done>Daily mid-game state saves on eliminate/guess/box, clears on solve, restores after clues render; random/replay untouched; build + suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify + test computeStats streak with local-keyed history (#209)</name>
  <files>src/completion.ts, tests/completion-stats.spec.ts</files>
  <read_first>
    - src/completion.ts (computeStats ~L45-78 streak walk; formatCountdown ~L83-93; renderCompletion signature + how it reads loadHistory then computeStats and renders the streak stat box)
    - tests/completion-links.spec.ts (analog: setupDOM, vi.resetModules, dynamic import, assert on [data-completion-*] innerHTML)
    - tests/setup.ts (localStorage cleared each test; document.title reset)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-PATTERNS.md (section "tests/completion-stats.spec.ts" — DOM setup, localStorage seed, streak-box index, indirect-test guidance)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-RESEARCH.md (Pattern: Streak Logic Analysis #209 — conclusion: no code change needed once dates are local; Pitfall 1 — never parse with T00:00:00Z)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-VALIDATION.md (critical path 5 — streak continuity scenarios)
  </read_first>
  <behavior>
    - Streak stays alive when today is unplayed: history newest-first [yesterday, day-before] (both consecutive) -> streak === 2 (counts from top of history, not from today). (#209 / Claude's-Discretion: keep alive.)
    - Consecutive run: history [d, d-1, d-2, d-3] -> streak === 4.
    - Real gap breaks streak: history [today, today-3, today-4] -> streak === 1 (gap of 3 days after the first entry stops the run).
    - Same-day duplicate does not inflate: two entries with identical date -> the duplicate does not add a day (streak does not double-count the same day).
    - Date parsing uses local midnight (T00:00:00, no Z) so a BST entry recorded near midnight is not shifted to the prior day (Pitfall 1) — assert a streak built from dates around a DST day still counts consecutively under TZ-sensitive system time.
  </behavior>
  <action>
    First INSPECT src/completion.ts computeStats: confirm it parses entry.date with 'T00:00:00' (no Z) at ~L59 and uses Math.round(diff/86400000) for day-diff. Per RESEARCH.md the streak logic is already correct once dates are local — so do NOT rewrite it. ONLY change code if inspection reveals an actual defect (e.g. a stray 'Z', or counting from today instead of from the top of history); if you change anything, document precisely what and why in the SUMMARY. Confirm formatCountdown still uses setHours(24,0,0,0) (local midnight — leave unchanged, D Claude's-Discretion countdown alignment). Then create tests/completion-stats.spec.ts following the completion-links.spec.ts analog: setupDOM() injecting the [data-completion-*] elements, vi.resetModules() + dynamic import('../src/completion.ts') in beforeEach, vi.useFakeTimers()/vi.setSystemTime() for deterministic "today", seed localStorage 'dlng_history' with the fixture arrays per the behavior cases, call renderCompletion(...) with representative args, and assert the rendered streak value in the streak stat box (locate the streak box per PATTERNS.md guidance — it is the 3rd stat box). Test computeStats INDIRECTLY via renderCompletion to avoid exporting a private function (export it only if indirect assertion is infeasible; document the decision). This proves #209 is healed.
  </action>
  <verify>
    <automated>npx vitest run tests/completion-stats.spec.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/completion-stats.spec.ts` exits 0 with all streak behavior cases passing.
    - `grep -c "T00:00:00Z" src/completion.ts` returns 0 in the streak walk (local parse preserved; no UTC shift — Pitfall 1).
    - `grep -c "setHours(24" src/completion.ts` returns 1 (countdown still local-midnight, unchanged).
    - If computeStats was modified, the SUMMARY states exactly what changed and why; if unchanged, the SUMMARY states "no code change — verified correct, covered by new tests".
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Streak behaviour proven by tests across alive/run/gap/duplicate/DST cases; computeStats confirmed correct (or minimally fixed with rationale); countdown unchanged; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage (dlng_active) -> app restore | Restored board state crosses into live game state. Validation happens in loadActive (Plan 04); app.ts trusts only its null/non-null result and re-fetches clues from the API. |
| localStorage (dlng_history) -> computeStats | History feeds the streak number. Read-only; no privilege or answer derived from it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | restored board state in app.ts | mitigate | app.ts only consumes loadActive()'s validated, non-null result (shape/version/date/size checked in Plan 04). Clues are re-fetched from the API on restore, so a tampered payload can never inject puzzle content or the answer (D-06). |
| T-05-13 | Information disclosure | persistence leaking into random/archive modes | mitigate | saveActive/loadActive are gated to daily mode; startRandomPuzzle/startReplayPuzzle never call them (D-08), asserted by code-read acceptance gate. |
| T-05-14 | Repudiation | streak miscount erodes trust | mitigate | computeStats parses local midnight (no Z) and is covered by alive/run/gap/duplicate tests; #209 phantom-gap cause removed by the canonical local date (Plans 01-02). |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH.md Package Legitimacy Audit: zero new packages this phase). |
</threat_model>

<verification>
- `npm run build` exits 0; `npx vitest run` shows no NEW failures vs the documented pre-existing router beacon failure.
- Save hooks present at the three mutation sites; clear on solve; restore after renderClues; random/replay never persist (grep + read gates).
- `npx vitest run tests/completion-stats.spec.ts` green across the streak scenarios.
- Countdown and streak-parse remain local-midnight (no Z, setHours(24) intact).
</verification>

<success_criteria>
- Reloading a mid-game daily puzzle restores the exact board + feedback (D-06, #206).
- State saves on eliminate/guess/box, clears on solve, never restores stale or non-daily state (D-07, D-08).
- Streak counts consecutive local days, stays alive when today unplayed, breaks on real gaps, ignores same-day dups (#209), proven by tests.
</success_criteria>

<output>
Create `.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-05-SUMMARY.md` when done. State whether computeStats was changed (and why) or confirmed unchanged, and list the final app.ts hook sites.
</output>
