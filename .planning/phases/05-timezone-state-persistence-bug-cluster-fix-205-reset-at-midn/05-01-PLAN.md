---
phase: 05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/date.ts
  - tests/date.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "A single client-side date module (src/date.ts) exports todayKey, puzzleNumberFor, localDateKey, and EPOCH_DATE (covers D-03)"
    - "todayKey() returns the browser-LOCAL YYYY-MM-DD date, not the UTC date, including on BST/GMT DST-transition days (covers D-01, D-02)"
    - "puzzleNumberFor(dateStr) is deterministic for a given date string and returns 1 for EPOCH_DATE"
    - "All new behaviour is proven by a passing vitest file (tests/date.spec.ts) covering >=3 timezones incl. one DST-transition day"
  artifacts:
    - path: "src/date.ts"
      provides: "Shared client date helpers — single source of truth for puzzle-day keying"
      exports: ["EPOCH_DATE", "todayKey", "puzzleNumberFor", "localDateKey"]
      contains: "DO NOT MODIFY"
    - path: "tests/date.spec.ts"
      provides: "Unit tests for date helpers across timezones + DST boundary"
      min_lines: 30
  key_links:
    - from: "src/date.ts"
      to: "Date (browser local clock)"
      via: "getFullYear/getMonth/getDate (local, never UTC getters, never toISOString for keying)"
      pattern: "getFullYear|getMonth|getDate"
---

<objective>
Create the single canonical client-side date module `src/date.ts`. This is the linchpin of the whole phase: three bugs (#205, #206, #209) share one root cause — the client has no single "puzzle day" helper, so date keying drifts between UTC and local across `app.ts` and `welcome.ts`. This plan builds the shared module + its full test suite. No consumer is rewired here (that is Plan 02) — the module ships first so consumers import a proven helper.

Purpose: Establish ONE local-midnight date helper (D-01, D-02, D-03) used everywhere on the client, removing the UTC-vs-local drift that causes the bug cluster.
Output: `src/date.ts` (pure, no imports), `tests/date.spec.ts` (green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-CONTEXT.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-RESEARCH.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-PATTERNS.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-VALIDATION.md

<naming_reconciliation>
RESEARCH.md and PATTERNS.md drafted this module as `src/date-helpers.ts` with functions `todayLocal` / `puzzleNumber` / `formatDate`. The AUTHORITATIVE contract for this phase (VALIDATION.md + the planner directive) renames them:

- Module file: `src/date.ts`  (NOT date-helpers.ts)
- `todayLocal()`      -> export `todayKey(): string`        (local YYYY-MM-DD "today")
- `puzzleNumber()`    -> export `puzzleNumberFor(dateStr: string): number`
- date-string builder -> export `localDateKey(d: Date): string` (formats any Date to local YYYY-MM-DD; `todayKey` calls `localDateKey(new Date())`)
- `formatDate()`      -> export `formatDate(dateStr: string): string` (keep this name; human-readable en-GB display string)
- `EPOCH_DATE`        -> export `EPOCH_DATE` (unchanged)

When RESEARCH/PATTERNS show old names, map them to the new names above. The behaviour is identical — only names change.
</naming_reconciliation>

<interfaces>
Public API this module MUST export (downstream plans import exactly these):

```typescript
export const EPOCH_DATE: string;                       // "2026-03-08" — DO NOT MODIFY (breaks puzzleNumberFor determinism)
export function localDateKey(d: Date): string;         // any Date -> local "YYYY-MM-DD"
export function todayKey(): string;                    // localDateKey(new Date())
export function puzzleNumberFor(dateStr: string): number; // deterministic; puzzleNumberFor(EPOCH_DATE) === 1
export function formatDate(dateStr: string): string;   // human display, en-GB, local-anchored
```
</interfaces>

<source_excerpts>
Exact source to copy the math from (VERIFIED in RESEARCH.md):

- Local-date builder + epoch + puzzleNumber math: `src/worker/puzzle.ts` L184-204 (copy the LOGIC, not the worker file).
- `formatDate` logic: `src/app.ts` L108-114.

puzzleNumberFor math (anchor both dates at T00:00:00Z so the SUBTRACTION is timezone-stable — this Z is correct here because it is epoch arithmetic on two fixed date strings, NOT date keying):
  ms = Date(dateStr + "T00:00:00Z") - Date(EPOCH_DATE + "T00:00:00Z"); return max(1, floor(ms/86400000) + 1)

formatDate parses with "T00:00:00" (NO Z) so the displayed day matches the local day:
  new Date(dateStr + "T00:00:00").toLocaleDateString("en-GB", { day:"numeric", month:"long", year:"numeric" })
</source_excerpts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for src/date.ts (RED)</name>
  <files>tests/date.spec.ts</files>
  <read_first>
    - tests/resolve-route.spec.ts (analog: pure-function unit-test structure, one assertion per `it`)
    - tests/setup.ts (global jsdom setup; localStorage cleared each test; fetch stubbed)
    - vitest.config.ts (environment: jsdom, globals: false, include: tests/**/*.spec.ts — note: spec.ts NOT test.ts; no per-file docblock needed, jsdom is the global env)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-PATTERNS.md (section "tests/date-helpers.spec.ts" — fake-timer + determinism patterns)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-VALIDATION.md (critical paths 1-2: BST/GMT transition, local-vs-UTC midnight gap)
  </read_first>
  <behavior>
    - todayKey() returns local date '2026-03-29' when system time is 2026-03-29T00:30:00+01:00 (BST: 00:30 local = 23:30 UTC on 2026-03-28). MUST NOT return '2026-03-28'.
    - todayKey() returns local date on the GMT fall-back day 2026-10-25T00:30:00 (clocks-go-back ambiguous hour) without shifting to the previous day.
    - todayKey() agrees with localDateKey(new Date()) at any fixed system time across at least 3 distinct offsets (e.g. UTC, +01:00 BST, -05:00).
    - localDateKey(new Date('2026-06-01T23:30:00-05:00')) returns the LOCAL day for that offset (zero-padded MM and DD).
    - puzzleNumberFor(EPOCH_DATE) === 1.
    - puzzleNumberFor('2026-03-09') === 2 (day after epoch).
    - puzzleNumberFor is deterministic: same dateStr -> same number on repeat calls.
    - formatDate('2026-06-01') contains '1 June 2026' (en-GB) and is stable regardless of system timezone (parsed local, no Z shift).
  </behavior>
  <action>
    Create tests/date.spec.ts. Import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest' and { EPOCH_DATE, todayKey, localDateKey, puzzleNumberFor, formatDate } from '../src/date.ts'. Use vi.useFakeTimers() in beforeEach and vi.useRealTimers() in afterEach for every test that depends on the wall clock; set the clock with vi.setSystemTime(new Date(...)) per the behavior cases. Group tests under describe blocks: "todayKey (DST boundary)", "localDateKey", "puzzleNumberFor", "formatDate". One assertion per it where practical. Cover all behavior cases above; the two DST-transition assertions (2026-03-29 BST spring-forward, 2026-10-25 GMT fall-back) are mandatory per VALIDATION.md critical paths. Do NOT use toISOString() in assertions for "today" expectations — that re-introduces the UTC bug; assert literal local date strings. This task implements D-03 verification and the #205 root-cause guard.
  </action>
  <verify>
    <automated>npx vitest run tests/date.spec.ts</automated>
  </verify>
  <done>tests/date.spec.ts exists; running it fails because src/date.ts does not yet exist (RED state — import resolution / missing exports error). The failure proves the tests are wired to the real module path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/date.ts (GREEN)</name>
  <files>src/date.ts</files>
  <read_first>
    - src/worker/puzzle.ts (L184-204 — the VERIFIED source for the local-date builder, EPOCH_DATE, and puzzleNumber math to copy the logic from; DO NOT import this file — strict client/worker boundary)
    - src/app.ts (L108-114 — source for formatDate logic)
    - tests/date.spec.ts (the contract this file must satisfy — written in Task 1)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-PATTERNS.md (section "src/date-helpers.ts" — copy-verbatim guidance + EPOCH "DO NOT MODIFY" comment)
  </read_first>
  <action>
    Create src/date.ts as a pure-computation module with ZERO imports (no browser-storage, no worker import — enforce the strict client/worker boundary; add a header comment: "Shared client date helpers. Pure computation. DO NOT import from worker/ — strict client/worker boundary."). Export: EPOCH_DATE (the string '2026-03-08' with comment "DO NOT MODIFY — breaks puzzleNumberFor determinism"); localDateKey(d: Date): string using d.getFullYear()/d.getMonth()+1/d.getDate() with String(...).padStart(2,'0') on month and day (LOCAL getters only — never getUTC*, never toISOString for keying); todayKey(): string returning localDateKey(new Date()); puzzleNumberFor(dateStr: string): number computing ms between Date(dateStr+'T00:00:00Z') and Date(EPOCH_DATE+'T00:00:00Z'), returning Math.max(1, Math.floor(ms/86400000)+1); formatDate(dateStr: string): string returning new Date(dateStr+'T00:00:00').toLocaleDateString('en-GB', { day:'numeric', month:'long', year:'numeric' }) (NO Z — local parse). Use the section-header comment style and explicit return types per CONVENTIONS. This file is the single canonical date source for D-01/D-02/D-03.
  </action>
  <verify>
    <automated>npx vitest run tests/date.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/date.spec.ts` exits 0 with all assertions passing (GREEN).
    - `grep -n "getUTC\|toISOString" src/date.ts` returns NO match inside localDateKey/todayKey (UTC getters forbidden for keying; if toISOString appears it must not be in a keying path).
    - `grep -c "export" src/date.ts` >= 5 (EPOCH_DATE + 4 functions exported).
    - src/date.ts has zero `import` statements: `grep -c "^import" src/date.ts` returns 0.
  </acceptance_criteria>
  <done>src/date.ts exists with the five exports; tests pass; no UTC getters in the keying path; module has no imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser local clock -> date.ts | The wall clock is user-controllable, but `todayKey` only reads it to pick the local puzzle day — no privilege or secret is gated on it. A user changing their clock only changes which puzzle they see (by design, Wordle-style D-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | EPOCH_DATE constant | mitigate | EPOCH_DATE carries a "DO NOT MODIFY" comment; puzzleNumberFor determinism is asserted in tests (puzzleNumberFor(EPOCH_DATE)===1) so an accidental change fails CI. |
| T-05-02 | Information disclosure | date.ts | accept | Pure date math; no secrets, no answer, no PII. A user setting their clock forward only selects a different local puzzle day — intended per D-01; the worker guard (Plan 03) bounds how far ahead a date is accepted. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH.md Package Legitimacy Audit: zero new packages this phase). |
</threat_model>

<verification>
- `npx vitest run tests/date.spec.ts` is green.
- DST-transition assertions (2026-03-29 spring-forward, 2026-10-25 fall-back) are present and pass.
- `npx vitest run` (full suite) shows no NEW failures vs the documented pre-existing failure (tests/router.spec.ts "navigate(/archive) calls navigator.sendBeacon" was already red before this phase — do not regress further).
</verification>

<success_criteria>
- src/date.ts is the single client date module exporting EPOCH_DATE, localDateKey, todayKey, puzzleNumberFor, formatDate.
- todayKey returns the local date (proven across >=3 timezones + 2 DST days).
- puzzleNumberFor is deterministic and returns 1 for EPOCH_DATE.
- No consumer is modified in this plan (consumers rewired in Plan 02).
</success_criteria>

<output>
Create `.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-01-SUMMARY.md` when done. Record the final export names and any deviation from the assumed signatures so Plan 02 imports exactly what shipped.
</output>
