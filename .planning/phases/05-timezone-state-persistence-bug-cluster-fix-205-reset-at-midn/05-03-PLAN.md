---
phase: 05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/date-guard.ts
  - src/worker/index.ts
  - tests/worker-guard.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The worker's future-puzzle guard accepts a date up to 1 calendar day ahead of the worker's UTC 'today', so a local-midnight player past their local midnight but before UTC midnight is NOT rejected (covers D-04, fixes #205 worker gate)"
    - "The guard still rejects dates 2+ days in the future (true-future protection retained)"
    - "The /solution guard (date >= todayLocal) is UNCHANGED — today's answer is never exposed early"
    - "No API request/response shape, route path, or puzzle-generation logic changes (covers D-05)"
    - "isFuturePuzzleDate is unit-tested via a pure exported helper across accept/reject boundaries"
  artifacts:
    - path: "src/worker/date-guard.ts"
      provides: "Pure, testable worker-side future-date guard with 1-day tolerance"
      exports: ["isFuturePuzzleDate"]
    - path: "src/worker/index.ts"
      provides: "handleGuess + GET /api/puzzle/:num use the tolerant guard; /solution unchanged"
      contains: "isFuturePuzzleDate"
    - path: "tests/worker-guard.spec.ts"
      provides: "Unit tests: accept today, accept today+1, reject today+2"
      min_lines: 20
  key_links:
    - from: "src/worker/index.ts"
      to: "src/worker/date-guard.ts"
      via: "import { isFuturePuzzleDate }"
      pattern: "isFuturePuzzleDate"
---

<objective>
Widen the worker's "future puzzle" guard by exactly one calendar day so a local-midnight player whose local date is one day ahead of the worker's UTC clock is no longer rejected (the #205 server gate). Extract the guard into a pure, testable `src/worker/date-guard.ts` helper, wire it into the two guards that should tolerate +1 day, and leave the `/solution` guard untouched.

Purpose: Implement D-04 (minimal worker date fix, ~1 day tolerance) without any API shape change (D-05). This is the only backend edit allowed this phase.
Output: src/worker/date-guard.ts (pure), index.ts guards rewired, tests green.

This plan is independent (depends on nothing) and runs in Wave 1 alongside Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-CONTEXT.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-RESEARCH.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-PATTERNS.md
@.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-VALIDATION.md

<boundary_note>
STRICT client/worker boundary: src/worker/date-guard.ts is WORKER-ONLY. It must NOT import src/date.ts (the client module). The worker keeps its own date logic. date-guard.ts may import the worker's existing todayLocal from src/worker/puzzle.ts (worker-internal) — that is allowed; only client<->worker cross-imports are forbidden.
</boundary_note>

<interfaces>
New worker helper (placed in its own file so vitest can import it without the Worker runtime):

```typescript
// src/worker/date-guard.ts
export function isFuturePuzzleDate(date: string): boolean;
// false if date <= worker-UTC-today OR date === worker-UTC-today+1 (one-day tolerance)
// true  if date >= worker-UTC-today+2  (true future — reject)
```
</interfaces>

<source_excerpts>
VERIFIED guard locations in src/worker/index.ts (from RESEARCH.md + PATTERNS.md):

- handleGuess guard, ~L101-103:  `if (body.date > todayLocal()) { return json({ error: 'Cannot guess future puzzles' }, 400); }`  -> replace condition with isFuturePuzzleDate(body.date).
- GET /api/puzzle/:num guard, ~L137:  `if (date > todayLocal()) return json({ error: 'Puzzle not available yet' }, 400);`  -> replace condition with isFuturePuzzleDate(date).
- GET /api/puzzle/:num/solution guard, ~L150:  `if (date >= todayLocal()) return json({ error: 'Solution not available' }, 403);`  -> LEAVE UNCHANGED (protects today's answer; >= not >, no tolerance allowed).

Worker todayLocal + date format regex `^\d{4}-\d{2}-\d{2}$` (input validation at ~L98) are unchanged. The guard only runs after format validation passes.

isFuturePuzzleDate logic (from RESEARCH.md "Worker Future-Puzzle Guard Fix"):
  todayUtc = worker todayLocal()  (UTC on Cloudflare)
  if (date <= todayUtc) return false
  tomorrow = Date(todayUtc + 'T00:00:00Z'); tomorrow.setUTCDate(getUTCDate()+1)
  tomorrowStr = tomorrow.toISOString().slice(0,10)
  return date > tomorrowStr
</source_excerpts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for isFuturePuzzleDate (RED)</name>
  <files>tests/worker-guard.spec.ts</files>
  <read_first>
    - tests/resolve-route.spec.ts (analog: pure-function unit test layout)
    - tests/setup.ts (global jsdom + fetch stub; vi available)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-PATTERNS.md (section "tests/worker-guard.spec.ts" — fake-timer accept/reject cases)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-VALIDATION.md (critical path 2 + worker-guard coverage target)
  </read_first>
  <behavior>
    - With system time 2026-05-29T12:00:00Z (worker UTC today = 2026-05-29): isFuturePuzzleDate('2026-05-29') === false (today accepted).
    - Same time: isFuturePuzzleDate('2026-05-28') === false (past accepted).
    - Same time: isFuturePuzzleDate('2026-05-30') === false (today+1 accepted — UTC+14 / local-midnight player edge, the #205 fix).
    - Same time: isFuturePuzzleDate('2026-05-31') === true (today+2 rejected — true future).
    - Month-boundary edge: system time 2026-05-31T23:00:00Z (UTC today = 2026-05-31): isFuturePuzzleDate('2026-06-01') === false (today+1 across month rollover) and isFuturePuzzleDate('2026-06-02') === true.
  </behavior>
  <action>
    Create tests/worker-guard.spec.ts. Import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest' and { isFuturePuzzleDate } from '../src/worker/date-guard.ts'. Use vi.useFakeTimers()/vi.setSystemTime(new Date('...Z'))/vi.useRealTimers(). Encode every behavior case above as its own `it`. The today+1 accept and today+2 reject pair is the mandatory core of the #205 fix. Include the month-rollover edge to prove setUTCDate arithmetic is correct.
  </action>
  <verify>
    <automated>npx vitest run tests/worker-guard.spec.ts</automated>
  </verify>
  <done>tests/worker-guard.spec.ts exists and FAILS because src/worker/date-guard.ts does not yet exist (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/worker/date-guard.ts and wire it into index.ts guards (GREEN)</name>
  <files>src/worker/date-guard.ts, src/worker/index.ts</files>
  <read_first>
    - src/worker/index.ts (guard sites ~L101-103, ~L137, ~L150; json() helper ~L31-35; date regex ~L98)
    - src/worker/puzzle.ts (L184-204 — worker todayLocal to reuse, worker-internal import is allowed)
    - tests/worker-guard.spec.ts (contract from Task 1)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-PATTERNS.md (section "src/worker/index.ts" — exact guard replacements + helper placement; note it suggests extracting to date-guard.ts for testability)
    - .planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-RESEARCH.md (Pitfall 6 — the +1 tolerance does NOT expose tomorrow's answer; puzzle generation is on-demand + deterministic)
  </read_first>
  <action>
    (1) Create src/worker/date-guard.ts exporting isFuturePuzzleDate(date: string): boolean. Get worker UTC today via the worker's own todayLocal (import from './puzzle.ts' — worker-internal, allowed) OR inline the same getFullYear/getMonth/getDate builder if importing causes a cycle; document the choice. Logic: if date <= todayUtc return false; compute tomorrow = new Date(todayUtc+'T00:00:00Z'), tomorrow.setUTCDate(tomorrow.getUTCDate()+1), tomorrowStr = tomorrow.toISOString().slice(0,10); return date > tomorrowStr. Add a header comment stating the +1 tolerance is deliberate (Phase 5 D-04) and that it does not expose tomorrow's answer (Pitfall 6). (2) In src/worker/index.ts: import isFuturePuzzleDate from './date-guard.ts'; replace the handleGuess guard condition (~L101) and the GET /api/puzzle/:num guard condition (~L137) with isFuturePuzzleDate(...); keep both error messages and status codes EXACTLY as-is (no API shape change, D-05). DO NOT touch the /solution guard at ~L150 (it stays `date >= todayLocal()`, status 403). Do not change the date-format regex, route paths, request body parsing, or puzzle generation.
  </action>
  <verify>
    <automated>npx vitest run tests/worker-guard.spec.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/worker-guard.spec.ts` exits 0 (all accept/reject cases pass).
    - `grep -c "isFuturePuzzleDate" src/worker/index.ts` returns >= 3 (1 import + 2 call sites).
    - `grep -c "date >= todayLocal" src/worker/index.ts` returns 1 (the /solution guard is preserved, unchanged).
    - `grep -cE "Cannot guess future puzzles|Puzzle not available yet|Solution not available" src/worker/index.ts` returns 3 (all three error strings intact — no API shape change).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>date-guard.ts exists and is unit-tested; the two tolerant guards use it; /solution guard unchanged; build green; no API shape change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Worker API (handleGuess, GET /api/puzzle/:num) | Untrusted `date` string crosses here. Format is validated by the existing regex BEFORE the guard runs; the guard bounds how far in the future a date may be. |
| Worker -> answer exposure (/api/puzzle/:num/solution) | The boundary protecting today's unsolved answer. Must stay strict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information disclosure | future-puzzle answer leak via widened guard | mitigate | Tolerance is bounded to exactly +1 calendar day and applies ONLY to the guess + clue-fetch guards. The /solution guard (`date >= todayLocal`) is explicitly left unchanged, so today's (and tomorrow's) answer is never served early. Acceptance gate asserts the /solution guard still present. (Pitfall 6: puzzle generation is on-demand + deterministic from date; serving clues for a +1 date leaks nothing the player could not compute by waiting.) |
| T-05-06 | Tampering | forged far-future date request | mitigate | isFuturePuzzleDate rejects date >= today+2 (unit-tested); the date-format regex rejects malformed input before the guard. |
| T-05-07 | Elevation/Spoofing | client spoofs date to access answer early | accept | Even with a forged +1 date, only clues are served (answer stays behind the untouched /solution guard); validation still runs server-side. No privilege gained. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH.md Package Legitimacy Audit: zero new packages this phase). |
</threat_model>

<verification>
- `npx vitest run tests/worker-guard.spec.ts` green: today / today-1 / today+1 accepted; today+2 rejected; month-rollover edge correct.
- `npm run build` exits 0.
- /solution guard unchanged (grep gate); all three error strings intact (no API shape change, D-05).
- Full `npx vitest run` shows no NEW failures vs the documented pre-existing router beacon failure.
</verification>

<success_criteria>
- Worker accepts a local-midnight player's +1-day date (#205 server gate fixed, D-04).
- Worker rejects true-future dates (>= +2 days).
- /solution answer protection unchanged; no route/contract/generation change (D-05).
- Pure isFuturePuzzleDate is unit-tested.
</success_criteria>

<output>
Create `.planning/phases/05-timezone-state-persistence-bug-cluster-fix-205-reset-at-midn/05-03-SUMMARY.md` when done. Record whether date-guard.ts imports worker todayLocal or inlines it, and confirm /solution was left untouched.
</output>
