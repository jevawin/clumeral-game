---
phase: 04-feedback-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tailwind.css
  - index.html
  - src/modals.ts
autonomous: false
requirements:
  - FBK-01
  - FBK-02
  - FBK-03
  - FBK-04
  - FBK-05

must_haves:
  truths:
    - "Clicking the feedback button in the game menu opens the feedback modal"
    - "The modal shows four category pills (General, Bug, Idea, Praise) with accent fill on the selected pill"
    - "Typing 400+ characters in the textarea shows a character counter in accent colour"
    - "The metadata line displays puzzle number, date, device, and browser"
    - "Submitting feedback closes the modal and shows a toast at the bottom centre"
    - "The modal opens with a fade + slide animation and closes with the reverse"
    - "The toast auto-dismisses after 3 seconds"
  artifacts:
    - path: "src/tailwind.css"
      provides: "Dialog transition rules, warn utility, toast-msg class, fb-cat selected state"
      contains: "data-fb-modal"
    - path: "index.html"
      provides: "Tailwind-styled feedback modal dialog markup"
      contains: "data-fb-modal"
    - path: "src/modals.ts"
      provides: "Toast element using toast-msg class instead of toast__msg"
      contains: "toast-msg"
  key_links:
    - from: "index.html"
      to: "src/modals.ts"
      via: "data-fb-* attributes and .fb-cat class"
      pattern: "data-fb-modal|data-fb-msg|data-fb-send|data-fb-counter|data-fb-meta|data-fb-cats|data-fb-modal-close|\\.fb-cat"
    - from: "src/tailwind.css"
      to: "index.html"
      via: "CSS rules targeting data-fb-modal and toast-msg"
      pattern: "data-fb-modal|toast-msg|warn"
    - from: "index.html [data-fb-btn]"
      to: "src/modals.ts openFeedback()"
      via: "click listener in initFeedbackModal"
      pattern: "data-fb-btn"
---

<objective>
Rewrite the feedback modal HTML in Tailwind and add supporting CSS rules so the modal opens, displays categories/textarea/metadata, submits, and shows a toast -- all using Tailwind utility classes and the existing modals.ts JS logic.

Purpose: Replace legacy BEM-styled feedback modal with Tailwind v4 classes, keeping the same data-attribute contract so modals.ts works without logic changes.
Output: Updated index.html (modal block), updated src/tailwind.css (dialog/toast/warn rules), updated src/modals.ts (toast class name).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-feedback-modal/04-CONTEXT.md
@.planning/phases/04-feedback-modal/04-RESEARCH.md
@.planning/phases/04-feedback-modal/04-UI-SPEC.md

<interfaces>
<!-- Key selectors and contracts the executor needs. Extracted from modals.ts. -->

From src/modals.ts (line 54):
```typescript
el.className = "toast__msg";  // CHANGE TO: "toast-msg"
```

From src/modals.ts (lines 71-82) -- DOM queries that MUST match new markup:
```typescript
const fbModalEl = document.querySelector('[data-fb-modal]') as HTMLDialogElement | null;
const closeBtn = document.querySelector('[data-fb-modal-close]');
const catBtns = modal.querySelectorAll(".fb-cat") as NodeListOf<HTMLButtonElement>;
const msgEl = document.querySelector('[data-fb-msg]') as HTMLTextAreaElement | null;
const counterEl = document.querySelector('[data-fb-counter]');
const metaEl = document.querySelector('[data-fb-meta]');
const sendBtn = document.querySelector('[data-fb-send]') as HTMLButtonElement | null;
const headerBtn = document.querySelector('[data-fb-header-btn]');
const footerBtn = document.querySelector('[data-fb-btn]');
```

From src/modals.ts (lines 113-125) -- counter classes toggled by JS:
```typescript
counterEl.classList.add("warn");
counterEl.classList.remove("hidden");
// and
counterEl.classList.add("hidden");
counterEl.classList.remove("warn");
```

From src/modals.ts (lines 87-98) -- open/close pattern:
```typescript
modal.showModal();
requestAnimationFrame(() => modal.classList.add("open"));
// close:
modal.classList.remove("open");
modal.addEventListener("transitionend", () => modal.close(), { once: true });
```

From src/modals.ts (line 104) -- active class toggled on pills:
```typescript
b.classList.toggle("active", selected);
```

From src/tailwind.css -- existing tokens:
```css
--color-bg, --color-text, --color-muted, --color-accent, --color-surface, --color-border
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dialog, toast, and utility CSS rules to tailwind.css</name>
  <files>src/tailwind.css</files>
  <read_first>
    - src/tailwind.css (current state -- 29 lines with @theme tokens and dark overrides)
    - .planning/phases/04-feedback-modal/04-UI-SPEC.md (animation contract, colour values)
    - .planning/phases/04-feedback-modal/04-RESEARCH.md (Pattern 2 dialog styles, Pattern 3 warn, Pattern 4 toast)
  </read_first>
  <action>
Add three blocks after the existing `@layer base` block in src/tailwind.css:

**Block 1: Dialog base styles in `@layer base`** (append to existing base block or add new one):

```css
/* Feedback modal dialog */
[data-fb-modal] {
  border: none;
  background: transparent;
  padding: 1rem;
  max-width: 100%;
  max-height: 100vh;
  width: 100%;
  height: 100%;
  margin: 0;
  opacity: 0;
  transition: opacity 200ms ease;
}
[data-fb-modal][open] {
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
}
[data-fb-modal].open {
  opacity: 1;
}
[data-fb-modal] [data-fb-modal-box] {
  transform: translateY(0.75rem);
  transition: transform 200ms ease;
}
[data-fb-modal].open [data-fb-modal-box] {
  transform: translateY(0);
}
[data-fb-modal]::backdrop {
  background: rgba(0, 0, 0, 0.55);
  opacity: 0;
  transition: opacity 200ms ease;
}
[data-fb-modal].open::backdrop {
  opacity: 1;
}
```

**Block 2: Component classes in `@layer utilities`:**

```css
@layer utilities {
  .warn { color: var(--color-accent); }

  .fb-cat[aria-checked="true"] {
    background-color: #0A850A;
    border-color: #0A850A;
    color: #fff;
  }
  html.dark .fb-cat[aria-checked="true"] {
    background-color: #1EAD52;
    border-color: #1EAD52;
  }

  .toast-msg {
    padding: 0.75rem 1rem;
    border-radius: 6px;
    background: var(--color-text);
    color: var(--color-bg);
    font-family: "DM Sans", system-ui, sans-serif;
    font-size: 1rem;
    font-weight: 600;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);
    pointer-events: auto;
    opacity: 0;
    transform: translateY(0.5rem);
    transition: opacity 250ms ease, transform 250ms ease;
  }
  .toast-msg.show {
    opacity: 1;
    transform: translateY(0);
  }
}
```

Per D-03: fade + slide animation at 200ms. Per D-04/D-05: toast restyled in Tailwind, positioned bottom centre. Per D-01: selected pill uses accent fill. The `.warn` class satisfies modals.ts `counterEl.classList.add("warn")` toggle. The `.fb-cat[aria-checked="true"]` rule satisfies the selected state without needing the `active` class for styling (the JS still toggles `active` but styling hooks on `aria-checked`).
  </action>
  <verify>
    <automated>cd /Users/jamiepersonal/Developer/clumeral-game && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/tailwind.css contains `[data-fb-modal]` (dialog base rule)
    - src/tailwind.css contains `[data-fb-modal].open` (open state rule)
    - src/tailwind.css contains `[data-fb-modal]::backdrop` (backdrop rule)
    - src/tailwind.css contains `.warn { color: var(--color-accent); }` (warn utility)
    - src/tailwind.css contains `.fb-cat[aria-checked="true"]` (selected pill rule)
    - src/tailwind.css contains `.toast-msg` (toast base class)
    - src/tailwind.css contains `.toast-msg.show` (toast visible state)
    - src/tailwind.css contains `background: rgba(0, 0, 0, 0.55)` (backdrop opacity)
    - src/tailwind.css contains `transition: opacity 200ms ease` (animation duration per D-03)
    - Vite build completes without errors
  </acceptance_criteria>
  <done>tailwind.css has dialog transition rules, warn utility, fb-cat selected state, and toast-msg class. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite feedback modal markup and update toast class in modals.ts</name>
  <files>index.html, src/modals.ts</files>
  <read_first>
    - index.html (lines 48-71 -- current modal markup to replace; line 282 -- toast container)
    - src/modals.ts (lines 50-62 -- showToast; lines 71-82 -- DOM queries; lines 100-125 -- resetForm/updateCounter)
    - src/tailwind.css (verify CSS rules from Task 1 are in place)
    - .planning/phases/04-feedback-modal/04-UI-SPEC.md (component inventory, copywriting contract, accessibility contract)
    - docs/CONVENTIONS.md (DOM patterns, accessibility)
  </read_first>
  <action>
**Part A: Replace index.html lines 48-71** (the `<!-- Feedback modal -->` block) with this Tailwind-styled markup:

```html
<!-- ── Feedback modal ── -->
<dialog data-fb-modal aria-labelledby="cw-fb-modal-title">
  <div data-fb-modal-box
       class="relative w-full max-w-[26rem] max-h-[90vh] overflow-y-auto rounded-md bg-surface shadow-[0_8px_40px_rgba(26,24,48,0.18)] dark:shadow-[0_8px_40px_rgba(0,0,0,0.5)] pt-7 px-6 pb-6">
    <button data-fb-modal-close
            class="absolute top-4 right-4 min-w-[44px] min-h-[44px] flex items-center justify-center bg-transparent border-none text-text/50 hover:text-text text-xl leading-none cursor-pointer"
            type="button" aria-label="Close feedback modal">&#10005;</button>
    <h2 id="cw-fb-modal-title" class="text-[26px] font-semibold leading-[1.2] text-text font-[DM_Sans] m-0">Send feedback</h2>
    <p class="text-muted text-base font-[DM_Sans] mt-1 mb-3">Let us know what you think.</p>

    <div data-fb-cats role="radiogroup" aria-label="Feedback category" class="flex flex-wrap gap-2 mb-3">
      <button class="fb-cat rounded-full border-[1.5px] border-border bg-surface text-text px-4 py-2 text-base font-[DM_Sans] cursor-pointer transition-[background-color,color,border-color] duration-150"
              data-cat="general" role="radio" aria-checked="true">General</button>
      <button class="fb-cat rounded-full border-[1.5px] border-border bg-surface text-text px-4 py-2 text-base font-[DM_Sans] cursor-pointer transition-[background-color,color,border-color] duration-150"
              data-cat="bug" role="radio" aria-checked="false">Bug</button>
      <button class="fb-cat rounded-full border-[1.5px] border-border bg-surface text-text px-4 py-2 text-base font-[DM_Sans] cursor-pointer transition-[background-color,color,border-color] duration-150"
              data-cat="suggestion" role="radio" aria-checked="false">Idea</button>
      <button class="fb-cat rounded-full border-[1.5px] border-border bg-surface text-text px-4 py-2 text-base font-[DM_Sans] cursor-pointer transition-[background-color,color,border-color] duration-150"
              data-cat="praise" role="radio" aria-checked="false">Praise</button>
    </div>

    <label for="cw-fb-msg" class="sr-only">Your feedback message</label>
    <textarea id="cw-fb-msg" data-fb-msg maxlength="500" rows="4"
              placeholder="Your message..."
              class="w-full min-h-[96px] max-h-[192px] resize-y border-[1.5px] border-border bg-surface text-text placeholder:text-muted p-3 rounded-[4px] font-[DM_Sans] text-base leading-relaxed focus:border-accent focus:outline-none transition-[border-color] duration-150"></textarea>
    <div data-fb-counter class="hidden w-full text-right font-[Inconsolata,monospace] text-base text-muted mt-1"></div>

    <p class="text-muted text-base font-[DM_Sans] mt-3 mb-2">This info will be sent with your feedback:</p>
    <div data-fb-meta class="font-[Inconsolata,monospace] text-base leading-[1.6] text-muted bg-[rgba(10,133,10,0.08)] dark:bg-[rgba(30,173,82,0.1)] p-2 px-3 rounded-[4px]"></div>

    <button data-fb-send
            class="w-full h-12 mt-3 bg-[#0A850A] text-white text-base font-semibold tracking-[0.04em] font-[DM_Sans] border-none rounded-[3px] shadow-[3px_3px_0_rgba(38,38,36,0.25)] cursor-pointer active:translate-y-0.5 active:translate-x-0.5 active:shadow-none disabled:opacity-50 disabled:cursor-not-allowed transition-[transform,box-shadow] duration-75">Send feedback</button>
  </div>
</dialog>
```

Key details per locked decisions:
- Per D-06: full markup rewrite with Tailwind utilities
- Per D-07: all data-fb-* attribute names preserved exactly
- Per D-01: pills are rounded-full capsule buttons (selected state handled by CSS rule in tailwind.css targeting `[aria-checked="true"]`)
- Per D-02: modal card uses bg-surface, rounded-md, shadow
- `.fb-cat` class kept on every pill button for the `modal.querySelectorAll(".fb-cat")` query in modals.ts
- `aria-labelledby="cw-fb-modal-title"` on dialog, matching heading id
- Close button has `aria-label="Close feedback modal"` per accessibility contract
- `maxlength="500"` on textarea per UI-SPEC

**Part B: Update toast container** at index.html line 282. Replace:
```html
<div class="toast" data-toast aria-live="polite"></div>
```
with:
```html
<div data-toast aria-live="polite" class="fixed bottom-6 left-1/2 -translate-x-1/2 z-[300] flex flex-col items-center gap-2 pointer-events-none"></div>
```
This positions the toast at bottom centre per D-05 using Tailwind utilities instead of the legacy `toast` class.

**Part C: Update src/modals.ts line 54.** Replace:
```typescript
el.className = "toast__msg";
```
with:
```typescript
el.className = "toast-msg";
```
This is the only JS change. The `toast-msg` class is defined in tailwind.css (Task 1) with all the styling and transition rules. The existing `el.classList.add("show")` on line 57 and `el.classList.remove("show")` on line 59 continue to work because tailwind.css defines `.toast-msg.show`.
  </action>
  <verify>
    <automated>cd /Users/jamiepersonal/Developer/clumeral-game && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `<dialog data-fb-modal aria-labelledby="cw-fb-modal-title">`
    - index.html contains `data-fb-modal-box` inside the dialog
    - index.html contains `data-fb-modal-close` with `aria-label="Close feedback modal"`
    - index.html contains `data-fb-cats` with `role="radiogroup"`
    - index.html contains exactly 4 elements with class `fb-cat` and `role="radio"`
    - index.html contains `data-cat="general"` with `aria-checked="true"` (default selected)
    - index.html contains `data-fb-msg` textarea with `maxlength="500"`
    - index.html contains `data-fb-counter` with class `hidden`
    - index.html contains `data-fb-meta`
    - index.html contains `data-fb-send` button
    - index.html contains `data-toast` with classes `fixed bottom-6 left-1/2 -translate-x-1/2 z-[300]`
    - index.html does NOT contain `fb-modal__box` (legacy class removed)
    - index.html does NOT contain `fb-modal__close` (legacy class removed)
    - index.html does NOT contain `fb-modal__send` (legacy class removed)
    - index.html does NOT contain `fb-counter` as a standalone class (uses Tailwind now)
    - index.html does NOT contain `class="toast"` (legacy toast class removed)
    - src/modals.ts contains `el.className = "toast-msg"` (not `toast__msg`)
    - src/modals.ts does NOT contain `toast__msg`
    - Vite build completes without errors
  </acceptance_criteria>
  <done>Feedback modal markup is fully rewritten with Tailwind classes. All data-fb-* attributes match modals.ts selectors. Toast container positioned with Tailwind. Toast element class updated in modals.ts. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of feedback modal and toast</name>
  <files>index.html, src/tailwind.css, src/modals.ts</files>
  <action>
Human visually verifies the feedback modal and toast in the browser. No code changes -- this is a verification checkpoint.

Steps:
1. Run `npm run dev` and open http://localhost:5173
2. Navigate to the game screen, open hamburger menu, click "Feedback"
3. Check modal opens with fade + slide-up animation over dark backdrop
4. Check modal card has rounded corners, surface background, shadow
5. Check "General" pill is selected (accent green fill), others have border-only style
6. Click "Bug" pill -- check it gets accent fill, "General" returns to border-only
7. Type in textarea -- check placeholder disappears, text in correct font
8. Type 400+ characters -- check counter appears showing "NNN/500" in accent colour
9. Check metadata line shows puzzle number, date, device, browser on accent-tinted background
10. Click "Send feedback" with text -- check button press effect, toast at bottom centre, modal closes
11. Check toast auto-dismisses after about 3 seconds
12. Toggle dark mode and repeat steps 2-6 -- check colours adapt correctly
13. Press Escape while modal is open -- check it closes with reverse animation
14. Click the close button (X) -- check it closes
  </action>
  <verify>
    <automated>cd /Users/jamiepersonal/Developer/clumeral-game && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <done>User confirms feedback modal looks correct, animations work, pills toggle, counter shows, toast appears and dismisses, dark mode works.</done>
</task>

</tasks>

<verification>
- `npx vite build` completes without errors
- index.html feedback modal block uses only Tailwind utility classes (no BEM classes like fb-modal__*)
- All data-fb-* attribute names match what modals.ts queries
- .fb-cat class preserved on pill buttons for JS querySelector
- Toast container uses fixed positioning via Tailwind
- modals.ts uses "toast-msg" class (not "toast__msg")
- tailwind.css contains dialog transition rules, warn utility, fb-cat selected state, toast-msg class
</verification>

<success_criteria>
- Feedback modal opens from game menu with fade + slide animation
- Four category pills render with accent fill on selected, border-only on unselected
- Textarea accepts input with character counter appearing at 400+ chars
- Metadata line shows correct puzzle info
- Submit sends to Google Apps Script (existing logic), shows success toast
- Toast appears at bottom centre and auto-dismisses
- Modal works in both light and dark mode
- No modals.ts logic changes beyond the one class name on line 54
</success_criteria>

<output>
After completion, create `.planning/phases/04-feedback-modal/04-01-SUMMARY.md`
</output>
