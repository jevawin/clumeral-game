---
phase: 02-filtering-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified: []
autonomous: false
requirements:
  - FILT-01
  - FILT-02
  - FILT-03
  - FILT-04
  - FILT-05
  - FILT-06
  - FILT-07
  - FILT-08
  - FILT-09
  - FILT-10

must_haves:
  truths:
    - "Running runFilterLoop 50 consecutive times in the browser always terminates — no hangs"
    - "50-run stress test produces no consecutive duplicate answers (randomness confirmed)"
    - "All 50 answers are JS numbers in range 100-999"
    - "runFilterLoop([row111]) terminates immediately and returns answer === 111"
    - "All 10 FILT requirement checks in the manual checklist are ticked green"
  artifacts:
    - path: ".planning/phases/02-filtering-engine/MANUAL-TEST-CHECKLIST.md"
      provides: "Signed-off test checklist with all boxes ticked"
  key_links:
    - from: "browser console"
      to: "runFilterLoop"
      via: "50-iteration stress test script"
      pattern: "results\\.length === 50"
---

<objective>
Human verification of the Phase 2 filter engine — run every browser-console test in the MANUAL-TEST-CHECKLIST.md and confirm all FILT-01 through FILT-10 requirements pass.

Purpose: The filter engine has no automated test runner. This checkpoint is the phase gate — all 10 requirements must be manually verified in the browser before Phase 3 begins.

Output: All checklist boxes ticked. Any failures reported back to Claude with the console output so bugs can be diagnosed and fixed.
</objective>

<execution_context>
@/Users/jamiepersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamiepersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-filtering-engine/MANUAL-TEST-CHECKLIST.md
@.planning/phases/02-filtering-engine/02-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run all FILT browser console checks</name>
  <action>
This is a human verification checkpoint. Claude has already implemented runFilterLoop() in app.js (Plan 01). The user must open the browser and run the manual test checklist to confirm all 10 FILT requirements pass. See <how-to-verify> for the exact steps.
  </action>
  <what-built>
runFilterLoop() pure function in app.js implementing the full filter loop algorithm (FILT-01 through FILT-10). The function accepts the 900-row gameRows array and returns { answer, clues } where answer is a 3-digit number and clues is a human-readable array.
  </what-built>
  <how-to-verify>
1. Start a local HTTP server from the project root:
   ```
   python3 -m http.server 8080
   ```

2. Open http://localhost:8080 in your browser.

3. Wait for the status to read "Ready — data loaded successfully."

4. Open the browser developer tools console (F12 or Cmd+Option+I).

5. Work through `.planning/phases/02-filtering-engine/MANUAL-TEST-CHECKLIST.md` section by section, pasting each console script and confirming the expected results.

   The checklist covers:
   - Pre-flight: gameRows.length === 900, SpecialNumbers column resolves to a text string
   - FILT-01: RANGE_GROUPS has 6 keys with correct column indices
   - FILT-02 + FILT-03: 5-run variability check (different answers each run)
   - FILT-04: Text clues use only '=' / '!=' operators; numeric clues use all 4
   - FILT-05 + FILT-06: 20-run check — no undefined answers
   - FILT-07 + FILT-08: 50-iteration stress test (all terminate, no consecutive duplicates) + row-111 pathological test (no hang)
   - FILT-09 + FILT-10: Clue shape { label, operator, value } and answer in 100-999

6. Tick each checkbox as it passes.

7. If any check FAILS: copy the failing console output and describe which FILT requirement is broken. Do NOT type "approved" — describe the failure instead.
  </how-to-verify>
  <verify>User types "approved" after all checklist items are ticked in MANUAL-TEST-CHECKLIST.md</verify>
  <done>All 10 FILT requirements verified in browser console. 50-run stress test passed. Row-111 pathological test passed. User has typed "approved".</done>
  <resume-signal>
Type "approved" if ALL checklist boxes are ticked and every test passed.

If any test failed, paste the failing console output and describe the issue (e.g., "FILT-08 hang on row 111" or "Pre-flight: gameRows[0][gameHeaders[4]] returned 1, not a string").
  </resume-signal>
</task>

</tasks>

<verification>
All items in MANUAL-TEST-CHECKLIST.md are ticked. The 50-iteration stress test passed. The pathological row-111 test terminated immediately. No console errors from runFilterLoop.
</verification>

<success_criteria>
- User types "approved" confirming all 10 FILT requirement checks passed
- 50-run stress test: all terminate, all 100-999, no consecutive duplicates
- Row-111 pathological test: terminates, answer === 111
- Phase 2 is complete and Phase 3 can begin
</success_criteria>

<output>
After completion, create `.planning/phases/02-filtering-engine/02-02-SUMMARY.md`
</output>
