---
phase: 06-add-guess-the-number-from-100-999-copy-to-welcome-play-scree
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/welcome.ts
  - index.html
autonomous: true
requirements: []

must_haves:
  truths:
    - "Welcome screen shows the line 'Work out the number from 100–999' above the Play button"
    - "Play (game) screen shows the line 'Work out the number from 100–999. Clues:' above the clue list"
    - "Both lines use the en-dash character (U+2013) in the range, matching shipped meta copy"
    - "Welcome line uses the existing subtitle classes (text-base text-text text-center)"
    - "Play line emphasises the 'Clues:' label and heads the clue list"
    - "The play line shows on all puzzles (daily and archive replay) with no conditional hide logic"
    - "On archive replay the archive banner row sits above the play copy line; visual order is banner → copy → clues"
    - "npm run build exits 0"
  artifacts:
    - path: "src/welcome.ts"
      provides: "Welcome range-copy line inside renderWelcome template"
      contains: "Work out the number from 100–999"
    - path: "index.html"
      provides: "Play range-copy line above [data-clue-list]"
      contains: "Work out the number from 100–999. Clues:"
  key_links:
    - from: "src/welcome.ts"
      to: "welcome screen subtitle styling"
      via: "shared classes text-base text-text text-center"
      pattern: "text-base text-text text-center"
    - from: "index.html play line"
      to: "[data-clue-list]"
      via: "static markup placed immediately above the clue list, after the archive-row"
      pattern: "data-clue-list"
---

<objective>
Add one line of explanatory range copy to two screens — welcome and play — telling
the player the answer is a 3-digit number from 100–999.

Purpose: Make the puzzle's answer range explicit to new and returning players, using
wording that already ships in the meta/OG tags (consistency).
Output: One new `<p>` in the welcome template (src/welcome.ts) and one new `<p>` in the
static game markup (index.html). Copy-only, frontend markup only. No backend, no puzzle
logic, no new components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-guess-the-number-from-100-999-copy-to-welcome-play-scree/06-CONTEXT.md

<interfaces>
<!-- Insertion points — executor uses these directly, no exploration needed. -->

Welcome (src/welcome.ts, renderWelcome template, lines 122-132):
- Render order inside the `max-w-[390px]` wrapper: heading + subtitle → OCTO_SVG → puzzleNumHtml → playButton() → htpSteps()
- New welcome line goes between `${puzzleNumHtml}` (line 129) and `${playButton()}` (line 130).
- Existing subtitle to copy classes from (line 126):
  `<p class="text-base text-text text-center">A daily number puzzle</p>`

Play (index.html, [data-screen="game"] > .max-w-[390px] wrapper, lines 189-221):
- Archive banner row `[data-archive-row]` at lines 192-195 (hidden by default, shown on replay).
- Clue list `[data-clue-list]` at line 198.
- New play line goes between the archive-row block (closes line 195) and the clue-list block (opens line 198) — so visual order is: archive banner (when shown) → play copy line → clues.

En-dash source of truth (already shipped, do NOT change):
- index.html:11 meta description: `Work out the number from 100–999. New puzzle every day.`
- index.html:17 og:description, index.html:25 twitter:description — same line.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add welcome range-copy line above the Play button</name>
  <files>src/welcome.ts</files>
  <read_first>
    - src/welcome.ts — the renderWelcome template (lines 122-132); copy the subtitle classes at line 126.
    - index.html:11 — the shipped meta description; copy the exact en-dash range `100–999` (U+2013) from here.
  </read_first>
  <action>
    In src/welcome.ts renderWelcome (per D-01), insert one new line into the template
    string between `${puzzleNumHtml}` (line 129) and `${playButton()}` (line 130).
    The line is a single `<p>` using the existing subtitle classes
    `text-base text-text text-center` (per D-08, matching line 126).
    Text content reads exactly `Work out the number from 100–999` (per D-03), with an
    en-dash U+2013 in the range (per D-07) — NOT a hyphen. Copy the en-dash character
    from index.html:11 to be certain. Do not change the existing subtitle "A daily number
    puzzle" (per D-02) or any other line.
  </action>
  <verify>
    <automated>grep -F "Work out the number from 100–999" src/welcome.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/welcome.ts contains the literal `Work out the number from 100–999`.
    - The range uses en-dash U+2013, not hyphen-minus U+002D. Verify with:
      `grep -F "100–999" src/welcome.ts` matches AND `grep -F "100-999" src/welcome.ts` does NOT match.
    - The new `<p>` carries the classes `text-base text-text text-center`.
    - The new line sits after `${puzzleNumHtml}` and before `${playButton()}` in the template.
    - The existing subtitle "A daily number puzzle" is unchanged (still present once).
  </acceptance_criteria>
  <done>Welcome template renders the range line, styled like the subtitle, above the Play button, with en-dash range.</done>
</task>

<task type="auto">
  <name>Task 2: Add play range-copy line above the clue list</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 188-221 — the [data-screen="game"] wrapper, archive-row (192-195), and clue-list (198).
    - index.html:11 — the shipped meta description; copy the exact en-dash range `100–999` (U+2013) from here.
  </read_first>
  <action>
    In index.html, inside the `[data-screen="game"] > .max-w-[390px]` wrapper (per D-04),
    insert one new `<p>` between the archive-row block (closes at line 195) and the
    clue-list block (opens at line 198). This is static markup with no conditional hide
    (per D-05) — it shows on daily and archive-replay puzzles alike. Because the archive
    banner row precedes it, the resulting visual order on replay is: archive banner → this
    copy line → clues (confirm this order; the banner row stays above the copy).
    Text reads exactly `Work out the number from 100–999. Clues:` (per D-06) with an
    en-dash U+2013 in the range (per D-07), copied from index.html:11. Emphasise the
    `Clues:` label since the line heads the clue list (per D-09): use a single `<p>` with
    the range phrase as body text and the `Clues:` label wrapped in an inline `font-bold`
    span (planner's pick — one `<p>` with inline-bold span is cleaner than a range/label
    split). Match the game-screen body type already used near the clues (Quicksand text-sm
    text-text, as on the archive banner at line 193) and add a small bottom margin (mb-4)
    so the line is visually separated from the clue list. Do not alter the archive-row or
    clue-list markup.
  </action>
  <verify>
    <automated>grep -F "Work out the number from 100–999. Clues:" index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains the literal `Work out the number from 100–999. Clues:`.
    - The range uses en-dash U+2013, not hyphen-minus. Verify: the new line matches
      `grep -F "100–999. Clues:" index.html` AND there is no `100-999. Clues:` (hyphen) match.
    - The `Clues:` label is wrapped in a `font-bold` span (emphasis per D-09).
    - The new `<p>` appears after the `[data-archive-row]` closing `</div>` (line 195) and
      before the `[data-clue-list]` opening div (line 198) in source order.
    - The archive-row and clue-list markup are unchanged.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Game screen shows the range + "Clues:" label line above the clue list on all puzzles; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced. This change is static text insertion only — no user input, no data flow,
no auth surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | static copy strings | accept | Both strings are hardcoded literals (not user-controlled), so no XSS/injection vector is introduced. No mitigation required. |

Realistic threat model: **None** — static text insertion, no attack surface.
No npm/pip/cargo installs in this phase, so the package-legitimacy gate does not apply.
</threat_model>

<verification>
1. `grep -F "Work out the number from 100–999" src/welcome.ts` matches (welcome line present, en-dash).
2. `grep -F "Work out the number from 100–999. Clues:" index.html` matches (play line present, en-dash).
3. Neither file contains a hyphen-minus variant: `grep -F "100-999" src/welcome.ts index.html` returns nothing for these new lines.
4. `npm run build` exits 0.
5. Visual order on archive replay: archive banner row → range/"Clues:" line → clue list.
</verification>

<success_criteria>
- Welcome screen displays `Work out the number from 100–999` above the Play button, styled like the subtitle.
- Game screen displays `Work out the number from 100–999. Clues:` above the clue list, with the `Clues:` label emphasised, on all puzzles.
- En-dash (U+2013) used in both lines, matching shipped meta copy.
- No backend, puzzle-logic, or component changes. Build passes.
</success_criteria>

<output>
Create `.planning/phases/06-add-guess-the-number-from-100-999-copy-to-welcome-play-scree/06-01-SUMMARY.md` when done.
</output>
