---
phase: 01-refinements-wave-1
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.html
  - src/completion.ts
autonomous: true
requirements:
  - CPY-01
  - CPY-02

must_haves:
  truths:
    - "Submit-guess button label reads 'Submit answer' (not 'Check my guess')"
    - "Solved-screen subheading reads 'Solved in N try' when N=1 and 'Solved in N tries' otherwise, with the existing tick icon retained"
  artifacts:
    - path: "index.html"
      provides: "Updated submit button text"
      contains: "Submit answer"
    - path: "src/completion.ts"
      provides: "Updated solved-screen subheading copy with correct singular/plural"
      contains: "Solved in"
  key_links:
    - from: "index.html submit button"
      to: "user click handler in app.ts"
      via: "[data-submit] selector — text change does not affect handler binding"
      pattern: "data-submit"
    - from: "completion.ts renderCompletion(tries)"
      to: "[data-completion-subheading]"
      via: "textContent assignment"
      pattern: "Solved in \\$\\{tries\\} tr"
---

<objective>
Tighten two pieces of user-facing copy: the submit-guess button label and the solved-screen subheading.

Purpose: CPY-01 and CPY-02 are pure text edits with no layout or behavior change. They share a "copy" theme but live in different files, and they're independent of menu/header (Plan 02), so this plan runs in parallel with Plan 02 in Wave 2.
Output: Updated submit button text in `index.html`, updated `renderCompletion()` subheading copy in `src/completion.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-refinements-wave-1/01-01-SUMMARY.md
@docs/CONVENTIONS.md
@index.html
@src/completion.ts
@src/app.ts

<interfaces>
<!-- The completion subheading element is in index.html line 270: -->
<!-- <p data-completion-subheading class="text-base text-text mt-1"></p> -->
<!-- (text-muted was changed to text-text in Plan 01-01.) -->

<!-- The completion subheading is populated by src/completion.ts line 94: -->
<!--   dom.subheading.textContent = `You got it in ${tries} ${tries === 1 ? 'try' : 'tries'}.`; -->
<!-- That string must change to: `Solved in ${tries} ${tries === 1 ? 'try' : 'tries'}` -->

<!-- The submit button is in index.html line 226: -->
<!-- <button data-submit ...>Check my guess</button> -->
<!-- The text "Check my guess" is the only thing changing; class list and data attributes stay. -->

<!-- The "tick icon" mentioned in CPY-02 is the ICON_CHECK SVG defined in src/app.ts line 295 — -->
<!-- but that is used for the inline FEEDBACK area on the game screen (renderFeedback, showCompletedState), -->
<!-- not the completion screen. The completion screen subheading is plain text. -->
<!-- CPY-02 says "keeping the existing tick icon" — interpret this as: do not remove any tick icons; -->
<!-- the inline `showCompletedState` tick remains, and `renderCompletion` continues to render its subheading -->
<!-- in the completion section without altering icon usage. Plan 04 (SLV-01/02/03) controls completion-screen layout. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Submit button copy + solved subheading copy</name>
  <files>index.html, src/completion.ts</files>

  <read_first>
    - index.html (line 226 — submit button)
    - src/completion.ts (lines 88–95 — `renderCompletion` heading + subheading)
    - src/app.ts (line 295 — ICON_CHECK definition, for context only; do not edit)
    - src/app.ts (lines 508–536 — `showCompletedState` uses ICON_CHECK; do not edit. CPY-02 "keep tick icon" refers to icons that already exist; we are not introducing or removing any.)
  </read_first>

  <action>
    Two edits.

    **A. `index.html` — change submit button label (CPY-01).**

    Line 226. Current:

    ```html
    <button data-submit class="w-full h-14 rounded-sm bg-accent text-white text-base font-bold tracking-wide font-[Quicksand] shadow-[3px_3px_0_rgba(38,38,36,0.18)] active:translate-x-[2px] active:translate-y-[2px] active:shadow-none" type="button">Check my guess</button>
    ```

    Change inner text from `Check my guess` to `Submit answer`. Keep all classes and attributes exactly as-is. Final inner text: `Submit answer`.

    **B. `src/completion.ts` — change subheading copy (CPY-02).**

    Lines 92–95 currently:

    ```typescript
    if (dom.heading) {
      dom.heading.textContent = isRandom ? 'Puzzle solved!' : `Puzzle #${puzzleNum} solved!`;
    }
    if (dom.subheading) {
      dom.subheading.textContent = `You got it in ${tries} ${tries === 1 ? 'try' : 'tries'}.`;
    }
    ```

    Change ONLY the subheading textContent expression. Final:

    ```typescript
    if (dom.subheading) {
      dom.subheading.textContent = `Solved in ${tries} ${tries === 1 ? 'try' : 'tries'}`;
    }
    ```

    Notes:
    - Drop the trailing period (REQUIREMENTS.md CPY-02 quotes "Solved in N try" / "Solved in N tries" with no full stop).
    - Keep the ternary that picks `try` vs `tries`.
    - Do NOT change the heading line above it (`Puzzle #N solved!`) — out of scope.
    - Do NOT touch `src/app.ts` — `showCompletedState` (the in-game inline solved message used when a returning solver loads `/`) writes its own copy via `dom.feedback.innerHTML`. That copy ("You already solved today's puzzle in N tries!") is being replaced indirectly by Plan 04 (SLV-02 auto-routes returning solvers straight to the completion screen, so the inline message is no longer reached on init). For now, leave it alone.

    Do NOT modify any other files.
  </action>

  <acceptance_criteria>
    - Run `grep -c 'Check my guess' index.html` — returns 0.
    - Run `grep -c '>Submit answer<' index.html` — returns exactly 1.
    - Run `grep -c 'data-submit' index.html` — returns at least 1 (button still exists).
    - Run `grep -c 'You got it in' src/completion.ts` — returns 0.
    - Run `grep -c 'Solved in \\${tries}' src/completion.ts` — returns 1.
    - The matched line in src/completion.ts contains both `'try'` and `'tries'` (the ternary is preserved): `grep -E "Solved in \\\$\\{tries\\}.*'try'.*'tries'" src/completion.ts` returns 1.
    - Run `npm run build` — exit code 0.
    - Manual: when the user solves the puzzle, the completion subheading shows `Solved in 1 try` (no full stop) for a 1-try win and `Solved in 3 tries` for a 3-try win.
  </acceptance_criteria>

  <verify>
    <automated>npm run build && [ "$(grep -c 'Check my guess' index.html)" = "0" ] && [ "$(grep -c '>Submit answer<' index.html)" = "1" ] && [ "$(grep -c 'You got it in' src/completion.ts)" = "0" ] && grep -q "Solved in \\\$\\{tries\\} \\\$\\{tries === 1 ? 'try' : 'tries'\\}" src/completion.ts</automated>
  </verify>

  <done>
    Submit button reads "Submit answer". Completion subheading reads "Solved in N try" (singular when N=1) / "Solved in N tries" otherwise, with no trailing period. No other markup or behaviour changed.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds.
- Submit button label and completion subheading copy match REQUIREMENTS.md verbatim.
</verification>

<success_criteria>
- CPY-01: Submit button reads "Submit answer".
- CPY-02: Completion subheading reads "Solved in N try" / "Solved in N tries" with the existing tick icons elsewhere on the page untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-refinements-wave-1/01-03-SUMMARY.md` noting the two literal text changes and confirming Plan 04 will replace the inline `showCompletedState` text path.
</output>
