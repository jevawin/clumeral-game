---
phase: 01-refinements-wave-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/tailwind.css
  - src/app.ts
  - src/welcome.ts
  - src/completion.ts
autonomous: true
requirements:
  - LAY-01
  - CLR-01
  - LNK-01

must_haves:
  truths:
    - "Footer sits at the bottom of the viewport on short pages and is pushed down by content on long pages — no `position: fixed` on the footer"
    - "No grey or muted text appears anywhere — every visible text uses --color-text (pure white in dark mode, pure black in light mode)"
    - "All inline links in the app use the standard accent-green link style (text in accent, 1px solid accent bottom border, 2px bottom padding, no text-decoration)"
  artifacts:
    - path: "index.html"
      provides: "Document-flow layout: body wraps all visible content in a min-h-screen flex flex-col with footer as last child"
      contains: "min-h-screen flex flex-col"
    - path: "src/tailwind.css"
      provides: "Standard `.link` utility class for inline links"
      contains: ".link"
  key_links:
    - from: "body shell in index.html"
      to: "footer"
      via: "flex column with footer as last child + flex-1 main wrapper"
      pattern: "min-h-screen flex flex-col.*footer"
    - from: "Inline links across screens"
      to: ".link utility"
      via: "class=\"link\""
      pattern: "class=\"[^\"]*\\blink\\b"
---

<objective>
Lay the structural and visual foundation for v1.1: footer in document flow, no muted text anywhere, and a single link style class used across the app.

Purpose: Plans 02–04 depend on these primitives (link style, text-text-only colors, normal layout flow). Get them right first so later plans only consume them.
Output: Updated `index.html` shell, `.link` utility in `src/tailwind.css`, all `text-muted` usages removed from `src/app.ts`, `src/welcome.ts`, `src/completion.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@docs/CONVENTIONS.md
@docs/DESIGN-SYSTEM.md
@index.html
@src/tailwind.css
@src/app.ts
@src/welcome.ts
@src/completion.ts

<interfaces>
<!-- Tailwind v4 design tokens already defined in src/tailwind.css @theme block -->
<!-- Use these directly — do not introduce new color tokens -->

```css
/* From src/tailwind.css @theme */
--color-bg:      #FAFAFA;  /* dark: #121213 */
--color-text:    #262624;  /* dark: #FAF8F4 — primary text */
--color-muted:   /* DO NOT USE — being removed via CLR-01 */
--color-accent:  #0A850A;  /* dark: #1EAD52 — links, accent */
--color-surface: #FFFFFF;  /* dark: #363634 — cards/inputs */
--color-border:  /* dark: rgba(246,240,232,0.1) */
```

Tailwind utilities generated: `bg-bg`, `text-text`, `text-accent`, `bg-accent`, `bg-surface`, `border-border`.
The `text-muted` utility WILL CONTINUE to compile (the token still exists for now) but MUST NOT appear in any markup or rendered HTML after this plan. Do NOT remove the `--color-muted` token from `@theme` in this plan — leave it; some component CSS (e.g. `[data-fb-modal]` placeholder text) still references it. Only delete `text-muted` and `placeholder:text-muted` *utility usages*.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Footer in document flow + .link utility</name>
  <files>index.html, src/tailwind.css</files>

  <read_first>
    - index.html (full file — current structure has TWO `<main>` elements and a fixed footer)
    - src/tailwind.css (lines 1–290, focus on `@layer base` and `@layer utilities`)
    - docs/DESIGN-SYSTEM.md (semantic colour tokens table)
    - docs/CONVENTIONS.md (DOM patterns — selectors via data-* attributes)
  </read_first>

  <action>
    Two coordinated edits.

    **A. `index.html` — wrap body in flex column, remove `position: fixed` from footer.**

    Current state (lines 40–289):
    - `<body>` opens
    - `<a class="skip-link">` (line 41)
    - `<div class="min-h-screen bg-bg">` (line 42) wrapping the legacy `<main class="max-w-[390px] ...">` header octo + the toast container
    - `<main data-screens class="fixed inset-0 z-10 pointer-events-none">` (line 137) wrapping the screens
    - `<footer data-footer class="fixed bottom-0 left-0 right-0 z-20 ...">` (line 286) — FIXED, must change

    Target state:
    - Keep the skip-link at the top of `<body>` exactly as is.
    - Replace the existing `<div class="min-h-screen bg-bg">` opening tag with `<div class="min-h-screen flex flex-col bg-bg">`. This div now wraps everything inside `<body>` except the skip-link.
    - Move BOTH `<main>` elements (the legacy header `<main class="max-w-[390px] ...">` AND the screens `<main data-screens ...>`) and the `<div data-toast ...>` toast container so they sit INSIDE this new flex column wrapper, before the footer.
    - Wrap the screens `<main data-screens>` so it occupies the available flex space. Replace `class="fixed inset-0 z-10 pointer-events-none"` on `<main data-screens>` with `class="relative flex-1 z-10"`. Remove `pointer-events-none` (the per-screen `pointer-events-none` / `pointer-events-auto` toggles in screens.ts continue to drive interactivity, see lines 30–33 of src/screens.ts). Inner `<section data-screen="...">` elements keep their existing classes — `absolute inset-0 ...` is still correct relative to the new `relative` parent.
    - Move `<footer data-footer ...>` INSIDE the flex column wrapper as its last child. Change its class from `fixed bottom-0 left-0 right-0 z-20 text-center py-4 text-muted text-sm font-[Quicksand]` to `mt-auto text-center py-4 text-text text-sm font-[Quicksand]`. Note: `mt-auto` pushes the footer to the bottom when content is short; `text-text` replaces `text-muted` per CLR-01.
    - Close the flex column wrapper `</div>` immediately before `<script type="module" src="/src/app.ts">`.

    Why `mt-auto` not `flex-1` on main: the screens `<main>` is `flex-1`, the footer uses `mt-auto` as a defensive guarantee. With `flex-1` on screens main and footer as the last child of `flex flex-col`, the footer naturally pins to the bottom when content is short and is pushed down when content is long.

    Do NOT change the legacy `<main class="max-w-[390px] mx-auto px-4">` header octo block (lines 79–131). It stays as a sibling above the screens main. Leave the toast container `<div data-toast ...>` (line 133) where it is in DOM order; it is `position: fixed` via its own classes so layout flow does not affect it.

    **B. `src/tailwind.css` — add `.link` utility inside `@layer utilities` (the existing layer at line 288).**

    Append AFTER the `.warn` rule and BEFORE the `.fb-cat[aria-checked="true"]` rule (i.e. inside `@layer utilities`, around line 290):

    ```css
    /* ─── Standard inline link style (LNK-01) ─── */
    /* Accent green text, 1px solid accent bottom border, 2px bottom padding,
       no text-decoration. Applied via class="link" on <a> elements. */
    .link {
      color: var(--color-accent);
      border-block-end: 1px solid var(--color-accent);
      padding-block-end: 2px;
      text-decoration: none;
    }
    .link:hover,
    .link:focus-visible {
      color: var(--color-accent);
      border-block-end-color: var(--color-accent);
    }
    ```

    Do NOT remove `--color-muted` from `@theme` (line 38) or `html.dark` (line 55) — Task 2 still needs the token to exist (some `placeholder:text-muted` references in the feedback modal will remain for placeholder text). Only utility *usages* in markup are being purged.

    **Important:** This task does NOT modify the feedback modal (`<dialog data-fb-modal>` and its children, lines 44–76 of index.html). That markup uses `text-muted` extensively but is the dedicated modal — purging will happen in Task 2 with explicit attention.
  </action>

  <acceptance_criteria>
    - Run `grep -n "position: fixed" index.html` — returns no matches in footer markup. The string `data-footer` and `fixed bottom-0` MUST NOT appear on the same line.
    - Run `grep -c "data-footer.*mt-auto" index.html` — returns 1.
    - Run `grep -c "data-footer.*text-muted" index.html` — returns 0.
    - Run `grep -c "min-h-screen flex flex-col bg-bg" index.html` — returns exactly 1.
    - Run `grep -c "main data-screens" index.html` — returns 1, and that line contains `relative flex-1` (not `fixed inset-0`).
    - Run `grep -n "main data-screens" index.html` — the matched line MUST NOT contain `fixed`.
    - Run `grep -c "\.link {" src/tailwind.css` — returns exactly 1.
    - Run `grep -c "border-block-end: 1px solid var(--color-accent)" src/tailwind.css` — returns at least 1.
    - The `.link` rule appears inside `@layer utilities { ... }` (verify by reading lines around the match — the closing `}` of `@layer utilities` is at the end of the file).
    - Run `npm run build` — completes with exit code 0 (no Tailwind/TypeScript errors).
    - Open the built site in dev (`npm run dev`) at 375×667 viewport: the footer is visible at the bottom of the welcome screen with no scroll, AND on a long page (game screen scrolled to bottom) the footer is below the content (not hovering).
  </acceptance_criteria>

  <verify>
    <automated>npm run build && grep -q "data-footer" index.html && ! grep -E "data-footer[^>]*\bfixed\b" index.html && grep -q "min-h-screen flex flex-col bg-bg" index.html && grep -q "^.link {" src/tailwind.css</automated>
  </verify>

  <done>
    Footer is in normal document flow, anchored at viewport bottom on short pages and pushed down on long pages. `.link` utility class exists in `src/tailwind.css` and resolves to `color: var(--color-accent)`, `border-block-end: 1px solid var(--color-accent)`, `padding-block-end: 2px`, `text-decoration: none`. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Purge text-muted from app, welcome, completion</name>
  <files>src/app.ts, src/welcome.ts, src/completion.ts, index.html</files>

  <read_first>
    - src/app.ts (focus lines 180, 334, 349–356, 372–381, 593–594)
    - src/welcome.ts (lines 97, 111, 113, 135)
    - src/completion.ts (line 84, lines 88–121)
    - index.html (feedback modal: lines 44–76; non-modal `text-muted` usages also exist in burger menu icons and stats sections — see Task 1 note. The MENU `text-muted` usages on icons inside the burger menu STAY for this plan; Plan 02 will replace them. The footer one was already fixed in Task 1.)
    - docs/DESIGN-SYSTEM.md (token table — confirms muted is being retired from text usage)
  </read_first>

  <action>
    Replace every `text-muted` (and `placeholder:text-muted`) class in the listed files with `text-text`, EXCEPT placeholder text (which keeps `placeholder:text-muted` for now — placeholder is not the foreground per CLR-01's intent of "all visible text").

    **Scope clarification:** CLR-01 says "all text uses only the foreground colors". Text input *placeholders* are a special intermediate UI state (not foreground text); leave `placeholder:text-muted` on `<textarea>`/`<input>`. All other `text-muted` usages on visible text MUST be replaced with `text-text`.

    **A. `src/app.ts` — replace these specific occurrences:**

    1. Line 180 (tag-tip popover close button):
       - From: `class="absolute top-1.5 right-1.5 p-0.5 rounded border border-border text-muted hover:text-text"`
       - To: `class="absolute top-1.5 right-1.5 p-0.5 rounded border border-border text-text hover:text-text"`

    2. Line 334 (history list `<li>` className):
       - From: `li.className = "font-mono text-base font-normal px-2 py-1 rounded-sm border border-border bg-surface text-muted";`
       - To: `li.className = "font-mono text-base font-normal px-2 py-1 rounded-sm border border-border bg-surface text-text";`

    3. Line 349 (`renderStats` heading):
       - From: `<p class="font-mono text-base font-bold uppercase tracking-widest text-muted mb-2">Your stats</p>`
       - To: `<p class="font-mono text-base font-bold uppercase tracking-widest text-text mb-2">Your stats</p>`

    4. Line 351 (`renderStats` "Played" label):
       - From: `<span class="text-sm text-muted font-[Quicksand]">Played</span>`
       - To: `<span class="text-sm text-text font-[Quicksand]">Played</span>`

    5. Line 352 (`renderStats` "Avg tries" label):
       - From: `<span class="text-sm text-muted font-[Quicksand]">Avg tries</span>`
       - To: `<span class="text-sm text-text font-[Quicksand]">Avg tries</span>`

    6. Line 354 (`renderStats` "Last N games"):
       - From: `<p class="text-sm text-muted mt-3 font-[Quicksand]">Last`
       - To: `<p class="text-sm text-text mt-3 font-[Quicksand]">Last`

    7. Line 372 (`renderStatsUpTo` heading):
       - From: `<p class="font-mono text-base font-bold uppercase tracking-widest text-muted mb-2">Your stats (to`
       - To: `<p class="font-mono text-base font-bold uppercase tracking-widest text-text mb-2">Your stats (to`

    8. Line 374 (`renderStatsUpTo` "Played"):
       - From: `<span class="text-sm text-muted font-[Quicksand]">Played</span>`
       - To: `<span class="text-sm text-text font-[Quicksand]">Played</span>`

    9. Line 375 (`renderStatsUpTo` "Avg tries"):
       - From: `<span class="text-sm text-muted font-[Quicksand]">Avg tries</span>`
       - To: `<span class="text-sm text-text font-[Quicksand]">Avg tries</span>`

    10. Line 377 (`renderStatsUpTo` "Last N games"):
        - From: `<p class="text-sm text-muted mt-3 font-[Quicksand]">Last`
        - To: `<p class="text-sm text-text mt-3 font-[Quicksand]">Last`

    11. Line 593 (replay puzzle archive label wrapper):
        - From: `label.className = "flex items-center gap-1 text-sm text-muted font-[Quicksand]";`
        - To: `label.className = "flex items-center gap-1 text-sm text-text font-[Quicksand]";`

    12. Line 594 (archive icon):
        - From: `<svg width="14" height="14" class="text-muted" aria-hidden="true">`
        - To: `<svg width="14" height="14" class="text-text" aria-hidden="true">`

    13. Lines 254–256 (`<p data-next>` is in index.html, not app.ts — handled in part D below).

    Also line 254 of index.html `<p data-next class="hidden mt-4 text-base text-muted font-[Quicksand]">` — see part D.

    **B. `src/welcome.ts` — replace these occurrences:**

    1. Line 97 (HTP step 2 arrow):
       - From: `<span class="text-xl text-muted shrink-0 self-center" aria-hidden="true">→</span>`
       - To: `<span class="text-xl text-text shrink-0 self-center" aria-hidden="true">→</span>`

    2. Line 111 ("Tap to remove"):
       - From: `<span class="text-sm text-muted text-center leading-tight">Tap to remove</span>`
       - To: `<span class="text-sm text-text text-center leading-tight">Tap to remove</span>`

    3. Line 113 (HTP second arrow):
       - From: `<span class="text-xl text-muted shrink-0 self-center" aria-hidden="true">→</span>`
       - To: `<span class="text-xl text-text shrink-0 self-center" aria-hidden="true">→</span>`

    4. Line 135 (welcome puzzle # subline):
       - From: `<p class="text-base text-muted text-center">Puzzle #${num} · ${formattedDate}</p>`
       - To: `<p class="text-base text-text text-center">Puzzle #${num} · ${formattedDate}</p>`

    **C. `src/completion.ts` — replace these occurrences:**

    1. Line 84 (`renderStatBox` label):
       - From: `<span class="text-sm text-muted mt-1 block">${label}</span>`
       - To: `<span class="text-sm text-text mt-1 block">${label}</span>`

    **D. `index.html` — non-modal `text-muted` usages on visible text:**

    1. Line 153 (game header puzzle label) — leave for now; Plan 02 (HDR-01) will delete this whole element.
    2. Line 230 (save row): `<div data-save class="items-center gap-1.5 mt-3.5 text-base text-muted font-[Quicksand]">` — replace `text-muted` with `text-text`. Final: `<div data-save class="items-center gap-1.5 mt-3.5 text-base text-text font-[Quicksand]">`.
    3. Line 246 ("Your guesses" heading): `<p class="font-mono text-base font-bold uppercase tracking-widest text-muted mb-2">Your guesses</p>` — replace `text-muted` with `text-text`.
    4. Line 254 (`<p data-next>`): replace `text-muted` with `text-text`. Final: `<p data-next class="hidden mt-4 text-base text-text font-[Quicksand]">`.
    5. Lines 269, 270 (completion heading + subheading already use `text-text` and `text-muted` respectively): `<p data-completion-subheading class="text-base text-muted mt-1"></p>` — replace `text-muted` with `text-text`.
    6. Line 276 (completion countdown): `<p data-completion-countdown class="text-base text-muted mt-8"></p>` — replace `text-muted` with `text-text`.

    **Out of scope (do NOT change in this plan):**
    - `<dialog data-fb-modal>` and its descendants (lines 44–76 of index.html) — feedback modal is internally consistent; muted usage there is for secondary modal copy. NOT covered by CLR-01's app-wide sweep in this plan.
    - The burger menu items' icon classes `<svg ... class="text-muted">` (lines 165, 169, 173, 177) — Plan 02 (MNU-03) will rebuild these styles; leave them alone here so that plan owns the change.

    **Verify the bg-accent/5 class in welcome.ts and app.ts (mini-digit indicators) is unchanged** — that is opacity-suffixed accent, not muted, and is correct.
  </action>

  <acceptance_criteria>
    - Run `grep -c "text-muted" src/app.ts` — returns exactly 0.
    - Run `grep -c "text-muted" src/welcome.ts` — returns exactly 0.
    - Run `grep -c "text-muted" src/completion.ts` — returns exactly 0.
    - Run `grep -c 'data-next.*text-muted' index.html` — returns 0.
    - Run `grep -c 'data-completion-subheading.*text-muted' index.html` — returns 0.
    - Run `grep -c 'data-completion-countdown.*text-muted' index.html` — returns 0.
    - Run `grep -c 'data-save.*text-muted' index.html` — returns 0.
    - Run `grep -c 'Your guesses' index.html` — returns 1, and that line contains `text-text` (not `text-muted`).
    - Run `grep -c 'data-fb-modal' index.html` — returns at least 1 (modal still present).
    - Run `grep -c 'placeholder:text-muted' index.html` — returns at least 1 (placeholder retention preserved on textarea line 67).
    - Run `npm run build` — exit code 0.
  </acceptance_criteria>

  <verify>
    <automated>npm run build && [ "$(grep -c text-muted src/app.ts src/welcome.ts src/completion.ts | grep -v ':0$' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>

  <done>
    No `text-muted` class appears in `src/app.ts`, `src/welcome.ts`, `src/completion.ts`, or in any non-modal, non-menu, non-placeholder location in `index.html`. All visible text outside the feedback modal and burger menu now uses `text-text`. Feedback modal and burger menu icons remain unchanged (Plan 02 scope). Build succeeds.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `npm run build` succeeds.
- `grep -RE "\\btext-muted\\b" src/app.ts src/welcome.ts src/completion.ts` returns no matches.
- `grep -E "data-footer[^>]*\\bfixed\\b" index.html` returns no matches.
- `.link` utility exists and is reachable in compiled CSS.
- Manual check: load the welcome screen, the game screen, and the completion screen — no light-grey text anywhere except inside the feedback modal (out of scope) and the burger menu icons (Plan 02 scope).
</verification>

<success_criteria>
- Footer in document flow (LAY-01) — visible at viewport bottom on short pages, pushed down on long pages, no `position: fixed`.
- Pure foreground only (CLR-01) — text-muted purged from `src/app.ts`, `src/welcome.ts`, `src/completion.ts`, and from non-modal/non-menu spots in `index.html`.
- Standard `.link` utility exists (LNK-01) — used by Plan 04 for "Show puzzle" / "Archive" links and by Plan 02 if menu items adopt it.
</success_criteria>

<output>
After completion, create `.planning/phases/01-refinements-wave-1/01-01-SUMMARY.md` documenting which files changed, which `text-muted` usages were left alone (modal, menu, placeholders) and why, and confirm `.link` utility is exported.
</output>
