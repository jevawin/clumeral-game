---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - style.css
  - .planning/phases/01-data-foundation/MANUAL-TEST-CHECKLIST.md
autonomous: true
requirements:
  - DATA-05

must_haves:
  truths:
    - "Opening index.html via a local HTTP server shows 'Loading...' in the browser"
    - "All six locked element IDs (#status, #clues, #guess, #submit, #history, #new-puzzle) exist in the DOM"
    - "Interactive elements (#guess, #submit, #new-puzzle) are disabled on initial load"
    - "PapaParse 5.4.1 CDN script tag is present and loads before app.js"
    - "style.css is linked and loads without errors (even if empty)"
  artifacts:
    - path: "index.html"
      provides: "Full HTML game shell — stable DOM contract for Phases 2 and 3"
      contains: "id=\"status\", id=\"clues\", id=\"guess\", id=\"submit\", id=\"history\", id=\"new-puzzle\""
    - path: "style.css"
      provides: "CSS entry point — empty in Phase 1, Phase 3 adds rules"
    - path: ".planning/phases/01-data-foundation/MANUAL-TEST-CHECKLIST.md"
      provides: "Step-by-step manual verification procedures for all 6 DATA requirements"
  key_links:
    - from: "index.html"
      to: "style.css"
      via: "<link rel=\"stylesheet\" href=\"style.css\">"
    - from: "index.html"
      to: "PapaParse CDN"
      via: "<script src=\"https://cdnjs.cloudflare.com/ajax/libs/PapaParse/5.4.1/papaparse.min.js\">"
    - from: "index.html"
      to: "app.js"
      via: "<script src=\"app.js\"> at end of <body>"
---

<objective>
Create the HTML game shell and CSS stub that establish the stable DOM contract for all subsequent phases, plus the manual test checklist used to verify Phase 1 completion.

Purpose: Every element ID defined here is the interface boundary between Phase 1 (data) and Phases 2–3 (logic and UI). Getting the shell right now means no DOM restructuring later. The manual checklist documents how a human verifier confirms all 6 DATA requirements pass in the browser.

Output: index.html (full shell), style.css (empty stub), MANUAL-TEST-CHECKLIST.md (verification procedures)
</objective>

<execution_context>
@/Users/jamiepersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamiepersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create index.html game shell and style.css stub</name>
  <files>index.html, style.css</files>
  <action>
Create index.html as the full game shell. Use this exact structure — every element ID is a Phase 2/3 contract, do not deviate:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>David Lark's Lame Number Game</title>
  <!-- DEVELOPMENT NOTE: This app uses fetch() to load data.csv.
       You MUST serve via a local HTTP server — opening index.html directly
       via file:// will block fetch() with a CORS error.
       Use: python3 -m http.server 8080  (then open http://localhost:8080)
       or VS Code Live Server extension. -->
  <link rel="stylesheet" href="style.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/PapaParse/5.4.1/papaparse.min.js"></script>
</head>
<body>
  <p id="status">Loading...</p>
  <ul id="clues"></ul>
  <input id="guess" type="text" inputmode="numeric" maxlength="3"
         placeholder="Enter a 3-digit number" disabled>
  <button id="submit" disabled>Submit</button>
  <ul id="history"></ul>
  <button id="new-puzzle" disabled>New Puzzle</button>

  <script src="app.js"></script>
</body>
</html>
```

Create style.css as an empty file with a single comment:
```css
/* Phase 3 adds all visual styling here. Phase 1 intentionally leaves this empty. */
```

Do NOT add any CSS rules in Phase 1 — Phase 3 owns all styling. Do NOT add a `defer` attribute to the PapaParse script (it must be parsed before app.js runs). The `<script src="app.js">` at the end of `<body>` handles timing safely.
  </action>
  <verify>
    <automated>MISSING — static browser app, no automated check possible. Human verification in plan 02's checkpoint.</automated>
  </verify>
  <done>index.html exists with all 6 element IDs (#status with "Loading..." text, #clues, #guess disabled, #submit disabled, #history, #new-puzzle disabled), PapaParse CDN script tag present, app.js script tag at end of body, style.css linked. style.css exists with only the comment line.</done>
</task>

<task type="auto">
  <name>Task 2: Create manual test checklist</name>
  <files>.planning/phases/01-data-foundation/MANUAL-TEST-CHECKLIST.md</files>
  <action>
Create the manual test checklist that a human uses to verify all 6 DATA requirements. This is the Wave 0 artifact required by VALIDATION.md.

Write the file with these exact sections:

# Phase 1 — Manual Test Checklist

**How to run:** Serve the project via local HTTP server before any test.
```
python3 -m http.server 8080
```
Then open http://localhost:8080 in Chrome or Firefox.

**Do NOT open index.html via double-click** — fetch() is blocked on file:// URLs.

---

## Pre-flight

- [ ] Local HTTP server is running (port 8080 or Live Server)
- [ ] Browser devtools are open (F12), Console and Network tabs visible
- [ ] data.csv is present in the project root

---

## DATA-01: CSV fetched on page load

1. Open http://localhost:8080
2. Open the Network tab in devtools
3. Reload the page
4. **PASS** if: `data.csv` appears in the Network tab with status 200
5. **FAIL** if: data.csv is absent or shows an error status

---

## DATA-02: Numeric columns are JavaScript numbers

1. Load http://localhost:8080
2. Wait for the console to show the "data.csv loaded" message
3. In the devtools Console, run: `typeof gameRows[0]['Number']`
4. **PASS** if: output is `"number"`
5. **FAIL** if: output is `"string"` or `undefined`

Also run: `typeof gameRows[0]['Attendance']`
**PASS** if: output is `"number"`

---

## DATA-03: All 23 headers have no trailing whitespace

1. Load http://localhost:8080
2. In the Console, run: `gameHeaders`
3. **PASS** if: no header string ends with a space character (inspect all 23)
4. **FAIL** if: any header has trailing/leading whitespace

Quick check — run: `gameHeaders.some(h => h !== h.trim())`
**PASS** if: output is `false`

Known affected headers (must be trimmed): indices 4, 13, 17, 22

---

## DATA-04: Row 0 of results is a data row, not a header string

1. Load http://localhost:8080
2. In the Console, run: `gameRows[0]`
3. **PASS** if: output is an object like `{ Number: 100, ... }` (numeric keys, object values)
4. **FAIL** if: output is a string like `"Number"` or a row with string-only values

Also run: `typeof gameRows[0]['Number']`
**PASS** if: `"number"`

---

## DATA-05: Loading indicator visible before data arrives

1. Open devtools → Network tab → set throttle to "Slow 3G"
2. Reload http://localhost:8080
3. **PASS** if: "Loading..." is visible in the page while the network request is in progress
4. **FAIL** if: the page is blank or shows an error before data.csv loads

Reset throttle to "No throttling" after this test.

---

## DATA-06: Error message shown when fetch fails

1. Rename `data.csv` to `data.csv.bak` in the project root
2. Reload http://localhost:8080
3. **PASS** if: the page shows an error message (e.g., "Error: could not load data.csv") — NOT a blank page, NOT "Loading..."
4. **FAIL** if: page is blank or stuck on "Loading..."

Restore after test: rename `data.csv.bak` back to `data.csv`

---

## Sign-Off

- [ ] DATA-01 PASS
- [ ] DATA-02 PASS
- [ ] DATA-03 PASS
- [ ] DATA-04 PASS
- [ ] DATA-05 PASS
- [ ] DATA-06 PASS

All 6 checks pass → Phase 1 complete. Proceed to `/gsd:verify-work`.
  </action>
  <verify>
    <automated>ls /Users/jamiepersonal/Developer/david-larks-lame-number-game/.planning/phases/01-data-foundation/MANUAL-TEST-CHECKLIST.md</automated>
  </verify>
  <done>MANUAL-TEST-CHECKLIST.md exists with sections for all 6 DATA requirements, each with numbered steps and explicit PASS/FAIL criteria. Pre-flight and sign-off sections present.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Confirm index.html opens in a browser via local server and shows "Loading..." with no console errors
2. Confirm style.css loads without errors (check Network tab — 200 status)
3. Confirm PapaParse CDN script loads (Network tab — papaparse.min.js status 200)
4. Confirm MANUAL-TEST-CHECKLIST.md exists and covers all 6 DATA requirements
</verification>

<success_criteria>
- index.html renders in browser showing "Loading..." text
- All 6 element IDs present and interactive elements disabled
- style.css loads without errors (empty/comment-only is correct)
- PapaParse 5.4.1 loads from CDN without errors
- MANUAL-TEST-CHECKLIST.md exists with steps for DATA-01 through DATA-06
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
