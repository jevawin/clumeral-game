---
phase: 03-game-loop-and-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - style.css
autonomous: true
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "Page has a near-black background with a radial gradient for depth"
    - "Game content sits inside a frosted-glass card with backdrop-filter blur"
    - "Card has a visible border and rounded corners"
    - "Accent color (orange/coral) appears on clue row left borders and interactive elements"
    - "Inter font (or system-ui fallback) renders throughout"
    - "Input and button are inline on desktop; stacked full-width below 480px"
    - "Correct feedback renders green; incorrect renders orange/coral"
    - "Wrong guesses appear in a muted/dimmed color"
    - "Both -webkit-backdrop-filter and backdrop-filter are present in CSS"
  artifacts:
    - path: "style.css"
      provides: "All visual styling for the game"
      contains: "-webkit-backdrop-filter"
  key_links:
    - from: "style.css .card"
      to: "index.html div.card"
      via: "class selector matches wrapper div added in Plan 01"
      pattern: "\\.card"
    - from: "style.css .clue-row"
      to: "app.js renderClues()"
      via: "li.className = 'clue-row' set in renderClues"
      pattern: "\\.clue-row"
    - from: "style.css .feedback--correct / .feedback--incorrect"
      to: "app.js renderFeedback()"
      via: "el.className = 'feedback feedback--correct' set in renderFeedback"
      pattern: "\\.feedback--"
---

<objective>
Write all visual styling in style.css: n8n-inspired dark theme, frosted-glass card, Inter font, responsive input row, and styled clue/feedback/history elements.

Purpose: Transform the bare HTML into the polished dark-theme game experience matching the locked design decisions.
Output: style.css fully populated. The game looks visually complete.
</objective>

<execution_context>
@/Users/jamiepersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamiepersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-game-loop-and-ui/03-CONTEXT.md
@.planning/phases/03-game-loop-and-ui/03-RESEARCH.md

<interfaces>
<!-- CSS class names set by app.js — style.css must target these exactly -->

From app.js renderClues():
  li.className = 'clue-row'           → style as .clue-row

From app.js renderFeedback():
  el.className = 'feedback'           → base state (no message)
  el.className = 'feedback feedback--correct'    → green correct message
  el.className = 'feedback feedback--incorrect'  → orange/coral incorrect message

From app.js renderHistory():
  li.className = 'history-item'       → muted/dimmed wrong guess number

<!-- HTML structure added in Plan 01 (index.html) -->

<h1 class="game-title">...</h1>           → title outside card
<div class="card">                         → frosted glass panel
  <p id="status">...</p>                   → muted subtitle inside card
  <ul id="clues">...</ul>                  → clue list
  <div class="input-row">                  → inline → stacked wrapper
    <input id="guess" ...>
    <button id="submit" ...>Submit</button>
  </div>
  <p id="feedback"></p>                    → color-coded feedback
  <p id="history-label" ...>...</p>        → "Previous guesses:" label
  <ul id="history">...</ul>               → wrong guess history
  <button id="new-puzzle" ...>New Puzzle</button>
</div>

<!-- CSS custom properties to use (from RESEARCH.md Pattern 4) -->

:root {
  --bg-deep:    #0f0f1a;
  --bg-card:    rgba(255, 255, 255, 0.06);
  --accent:     #ff6d5a;
  --accent-alt: #ff914d;
  --text:       #e8e8f0;
  --text-muted: #7a7a9a;
  --green:      #4caf88;
  --red:        #ff6d5a;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write style.css — dark theme, frosted glass card, typography</name>
  <files>style.css</files>
  <action>
Write the complete style.css. The file is currently empty — write it in full.

Structure the file in this order with comments marking each section:

**1. CSS custom properties**
```css
:root {
  --bg-deep:    #0f0f1a;
  --bg-card:    rgba(255, 255, 255, 0.06);
  --accent:     #ff6d5a;
  --accent-alt: #ff914d;
  --text:       #e8e8f0;
  --text-muted: #7a7a9a;
  --green:      #4caf88;
  --red:        #ff6d5a;
}
```

**2. Reset / base**
```css
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}
```

**3. Body + background**
```css
body {
  background-color: var(--bg-deep);
  background-image: radial-gradient(ellipse at top, #1a1a2e 0%, #0f0f1a 70%);
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-family: 'Inter', system-ui, -apple-system, sans-serif;
  color: var(--text);
  padding: 1.5rem;
  gap: 1.25rem;
}
```

**4. Game title (outside card)**
```css
.game-title {
  font-size: 1.1rem;
  font-weight: 500;
  color: var(--text-muted);
  letter-spacing: 0.02em;
  text-align: center;
}
```

**5. Frosted glass card — CRITICAL: both -webkit-backdrop-filter AND backdrop-filter required (UI-02)**
```css
.card {
  background: var(--bg-card);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 16px;
  padding: 2rem 2.5rem;
  width: min(480px, 90vw);
  -webkit-backdrop-filter: blur(12px);  /* Safari — required per UI-02 */
  backdrop-filter: blur(12px);
  display: flex;
  flex-direction: column;
  gap: 1rem;
}
```

**6. Status subtitle**
```css
#status {
  font-size: 0.8rem;
  color: var(--text-muted);
  font-weight: 400;
}
```

**7. Clue list and clue rows**
```css
#clues {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 0;
}

.clue-row {
  border-left: 3px solid var(--accent);
  padding: 0.35rem 0 0.35rem 0.75rem;
  margin-bottom: 0.4rem;
  font-size: 0.95rem;
  line-height: 1.4;
}
```

**8. Input row — inline desktop, stacked mobile**
```css
.input-row {
  display: flex;
  gap: 0.5rem;
  align-items: center;
}

#guess {
  flex: 1;
  padding: 0.55rem 0.75rem;
  background: rgba(255, 255, 255, 0.08);
  border: 1px solid rgba(255, 255, 255, 0.15);
  border-radius: 8px;
  color: var(--text);
  font-family: inherit;
  font-size: 1rem;
  outline: none;
}

#guess:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 2px rgba(255, 109, 90, 0.25);
}

#guess:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

#submit {
  padding: 0.55rem 1.1rem;
  background: var(--accent);
  color: #fff;
  border: none;
  border-radius: 8px;
  font-family: inherit;
  font-size: 0.95rem;
  font-weight: 500;
  cursor: pointer;
  transition: background 0.15s ease;
}

#submit:hover:not(:disabled) {
  background: var(--accent-alt);
}

#submit:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

@media (max-width: 480px) {
  .input-row {
    flex-direction: column;
  }
  .input-row button {
    width: 100%;
  }
}
```

**9. Feedback message**
```css
.feedback {
  font-size: 0.95rem;
  font-weight: 500;
  min-height: 1.4rem;
}

.feedback--correct {
  color: var(--green);
}

.feedback--incorrect {
  color: var(--red);
}
```

**10. Guess history**
```css
#history-label {
  font-size: 0.8rem;
  color: var(--text-muted);
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

#history {
  list-style: none;
  display: flex;
  flex-wrap: wrap;
  gap: 0.4rem;
}

.history-item {
  color: var(--text-muted);
  font-size: 0.9rem;
  padding: 0.15rem 0;
}
```

**11. New Puzzle button**
```css
#new-puzzle {
  padding: 0.5rem 1rem;
  background: transparent;
  color: var(--accent);
  border: 1px solid var(--accent);
  border-radius: 8px;
  font-family: inherit;
  font-size: 0.9rem;
  font-weight: 500;
  cursor: pointer;
  transition: background 0.15s ease, color 0.15s ease;
  align-self: flex-start;
}

#new-puzzle:hover:not(:disabled) {
  background: var(--accent);
  color: #fff;
}

#new-puzzle:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}
```

Pitfall guards:
- The card MUST use `rgba()` background (not a solid color) or backdrop-filter is invisible.
- BOTH `-webkit-backdrop-filter: blur(12px)` and `backdrop-filter: blur(12px)` must be present on `.card`.
- Do NOT use `innerHTML` in CSS context — this is not applicable; just write the CSS as specified.
  </action>
  <verify>
    <automated>grep -c '\-webkit-backdrop-filter' /Users/jamiepersonal/Developer/david-larks-lame-number-game/style.css && grep -c 'backdrop-filter: blur' /Users/jamiepersonal/Developer/david-larks-lame-number-game/style.css && grep -c '\-\-accent' /Users/jamiepersonal/Developer/david-larks-lame-number-game/style.css && grep -c '\.clue-row' /Users/jamiepersonal/Developer/david-larks-lame-number-game/style.css && grep -c '\.feedback--correct' /Users/jamiepersonal/Developer/david-larks-lame-number-game/style.css && grep -c '@media.*480px' /Users/jamiepersonal/Developer/david-larks-lame-number-game/style.css</automated>
  </verify>
  <done>style.css contains: CSS custom properties with all --accent/--bg/--text/--green variables; .card with both -webkit-backdrop-filter and backdrop-filter; .clue-row with left border; .feedback--correct and .feedback--incorrect color classes; .input-row with @media 480px stacking rule; #new-puzzle outline button style.</done>
</task>

</tasks>

<verification>
After completing the CSS task, open the game at http://localhost:8080 (start server if not running: `python3 -m http.server 8080` from the project root) and visually confirm:

1. Body has a near-black background — not white or default browser color (UI-01)
2. Game content is inside a visible card with slightly lighter/translucent background and rounded corners (UI-02)
3. On Chrome/Firefox: card shows frosted blur effect when page background is visible through it
4. On Safari (if available): frosted glass also works (both -webkit-backdrop-filter and backdrop-filter present)
5. Clue rows have an orange/coral left border (UI-01)
6. Inter font renders (DevTools > Network > Font tab shows Inter loading, or text looks like Inter)
7. Shrink browser below 480px width: input and Submit button stack vertically (UI-03)

Code check:
```
grep -n 'webkit-backdrop-filter\|backdrop-filter' style.css
```
Must show BOTH lines.
</verification>

<success_criteria>
- style.css has all 11 sections; file is non-empty
- Both -webkit-backdrop-filter and backdrop-filter present on .card
- --bg-card uses rgba() not a solid color
- Dark theme visually matches n8n-inspired spec (near-black bg, orange/coral accents)
- Responsive breakpoint at 480px collapses input row
- All UI-01, UI-02, UI-03 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-loop-and-ui/03-02-SUMMARY.md`
</output>
